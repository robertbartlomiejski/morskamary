# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Virtual environments
venv/
ENV/

# Large binary files (GitHub limit: 100MB per file)
*.pdf
*.PDF
*.docx
*.DOCX
*.xlsx
*.XLSX
docs/literature/
docs/policy/
manuscripts/

# Node modules
node_modules/
package-lock.json
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# GitHub configuration exports
bluesociology.json

# Data (local, not in repo)
data/local/
*.csv.bak

# Azurite local emulator state (machine-generated)
.azurite/
__azurite_db*.json
__blobstorage__/
__queuestorage__/
__tablestorage__/

# Generated analysis outputs are now tracked in git
# (run python run_full_analysis.py to regenerate)
# outputs/*.html
# outputs/*.json
# outputs/*.csv
outputs/sectors/
# Transient smoke-test artifact — generated at runtime, uploaded as CI artifact,
# not committed; exclude so generate_manifest.py and sync_audit see a clean tree.
outputs/research_api_smoke_report.json
# outputs/sector_dictionaries/ — now tracked as committed artifacts (PR#98)
# deep_update_patch bundle: generated outputs are regenerated at runtime
deep_update_patch/outputs/

# Secrets and credentials — NEVER commit these
.env
.env.*
.env.ps1
.secrets/
*oauth*.json
*credentials*.json
!schemas/dynamic_credentials.schema.json
!outputs/credentials_database.json
!outputs/credentials_dynamic_database.json
!outputs/credentials_generation_rationale.json
*token*.json
config/research_sources.local.yml
src/morskamary/
