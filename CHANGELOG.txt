CHANGELOG.txt

Purpose
This file records what changed, when it changed, and why, to support traceability across manuscripts, datasets, and competence mappings.

Entry format
Date in YYYY-MM-DD
Change type add update fix remove rename
Scope policy dataset draft manuscript governance scripts
Files affected exact file names
Summary one to three sentences
Reason rationale for the change
Impact what downstream tables figures or credentials are affected
Provenance parent sources added or changed
Quality checks performed and outcomes
Reviewer if any

Entries
Unreleased
Use this section for changes in progress that are not yet merged into the main branch. Move items to dated entries when merged.

2026-03-10
Change type fix add
Scope governance scripts
Files affected .github/workflows/blank.yml .github/workflows/ci.yml .vscode/settings.json .vscode/launch.json .vscode/tasks.json src/core.py src/competence_mapper.py
Summary Added black --check and mypy to blank.yml CI workflow so formatting and type safety are verified automatically on every push. Fixed ci.yml manifest determinism check to use the correct output filename MANIFEST_SOURCES.csv instead of non-existent manifest.json/txt candidates. Auto-reformatted src/core.py and src/competence_mapper.py with black (line-length=88). Fixed VS Code settings: enabled pytest, disabled misconfigured unittest, configured black as the default formatter with format-on-save. Resolved unresolved git merge conflict in launch.json, keeping the Python debugpy configuration. Fixed Windows-style backslash path in tasks.json to use forward slash.
Reason CI lacked formatting and type-safety checks; manifest check was always failing because it searched for wrong file names; VS Code was using wrong test runner and had no auto-formatter configured; launch.json contained raw merge conflict markers making it invalid JSON.
Impact CI now reliably enforces code quality (formatting + linting + type safety + tests); manifest reproducibility check will pass; developers get format-on-save in VS Code; debugger works correctly.
Provenance None (CI/tooling fixes and auto-formatting only).
Quality checks black --check passes; flake8 passes; mypy passes; pytest tests/ passes (8/8).
Reviewer GitHub Copilot Agent

2026-03-09
Change type fix add
Scope scripts governance
Files affected requirements.txt Dockerfile CONTRIBUTING.md
Summary Fixed openpyxl version constraint in requirements.txt (changed >=3.8.0 to >=3.0.0; version 3.8.0 does not exist). Fixed Windows-style path in Dockerfile CMD (backslash → forward slash). Added CONTRIBUTING.md with step-by-step getting-started guide for new contributors.
Reason requirements.txt blocked pip install entirely; Dockerfile CMD would fail on Linux containers; no onboarding guide existed for new contributors.
Impact pip install -r requirements.txt now succeeds; Docker build runs correctly; new contributors can set up and verify the environment in minutes.
Provenance None (bug fixes and new documentation only).
Quality checks pip install verified; pytest tests/ passes (8/8); CONTRIBUTING.md reviewed for accuracy against pyproject.toml and existing tooling.
Reviewer GitHub Copilot Agent

2026-02-18
Change type add
Scope governance
Files affected CITATION.txt DATA_GOVERNANCE.txt LLM_CONTEXT_INSTRUCTION.txt CHANGELOG.txt
Summary Added repository governance and citation scaffolding to support reproducible competence mapping and micro-credential design.
Reason Establish traceability rules and evidence discipline for Blue Sociology outputs.
Impact Improves auditability of future derived datasets and manuscript revisions.
Provenance None (new files created from scratch).
Quality checks Manual review for internal consistency and alignment with repository sources referenced in the governance files.
Reviewer Robert Bartłomiejski
text
