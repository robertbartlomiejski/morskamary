CHANGELOG.txt

Purpose
This file records what changed, when it changed, and why, to support traceability across manuscripts, datasets, and competence mappings.

Entry format
Date in YYYY-MM-DD
Change type add update fix remove rename
Scope policy dataset draft manuscript governance scripts
Files affected exact file names
Summary one to three sentences
Reason rationale for the change
Impact what downstream tables figures or credentials are affected
Provenance parent sources added or changed
Quality checks performed and outcomes
Reviewer if any

Entries
Unreleased
Use this section for changes in progress that are not yet merged into the main branch. Move items to dated entries when merged.

- 2026-07-16 (fix: resolve PR-191 post-merge regressions across Layer 1/3/5 hypothesis and provider contracts)
  Change type fix
  Scope scripts tests governance
  Files affected MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Resolved PR-191 post-merge regressions affecting Layer 1/3/5 hypothesis and provider contracts. Regenerated MANIFEST_SOURCES.csv for reproducibility gate compliance.
  Reason Post-merge regressions from PR-191 caused CI governance checks to fail due to stale manifest and missing changelog entry.
  Impact CI governance and reproducibility gates now pass on the fix branch.

- 2026-07-13 (fix: replace dynamic CodeQL setup with repo-managed workflow)
  Change type fix add
  Scope governance CI tests
  Files affected .github/workflows/codeql.yml tests/test_codeql_workflow.py CHANGELOG.txt MANIFEST_SOURCES.csv
  Summary Added an explicit repository-managed CodeQL workflow for the `actions` and `python` languages with `build-mode: none`, replacing reliance on GitHub's generated `dynamic/github-code-scanning/codeql` configuration. Added regression tests to keep the workflow checked into source control and to assert that Python analysis stays on the no-build path without an `autobuild` step.
  Reason Code scanning job `Analyze (python)` in run `29253074083` failed during job setup while resolving the generated dynamic workflow actions (`Service Unavailable`) before any repository step ran. Moving CodeQL configuration into the repository makes the Python analysis path explicit, reviewable, and less dependent on opaque generated workflow behavior.
  Impact Code scanning for Python and Actions is now source-controlled and reproducible from the repository itself; future workflow changes can be validated by tests in normal CI.
  Provenance GitHub Actions run `29253074083`, job `86826081347`; repository workflow pattern in `.github/workflows/dependency-submission.yml`.
  Quality checks python -m pytest tests/test_codeql_workflow.py tests/test_ci_workflow.py -q; python scripts/generate_manifest.py; python -m flake8 tests/test_codeql_workflow.py; python -m pytest tests/ -q.

- 2026-07-15 (fix: harden package preflight checksum/path/evidence controls)
  Change type fix
  Scope scripts src tests governance
  Files affected scripts/build_live_cumulative_release_package.py src/scientific_sources/derived_competence_analysis.py tests/test_live_cumulative_release_package.py tests/test_derived_competence_analysis.py CHANGELOG.txt
  Summary Strengthened release-package preflight by requiring checksum coverage for all required Layer 0-5 inputs, enforcing the exact `outputs/live_runs/<run_id>/raw/raw_acquisition_index.csv` run-path contract, and rejecting derived-demand rows missing supporting `evidence_ids`. Layer 5 learning outcomes now serialize explicit `unavailable` evidence IDs instead of an ambiguous empty value.
  Reason PR-191 follow-up review identified remaining blockers where package preflight could pass incomplete checksum declarations, accept weaker raw-index path patterns, and preserve ambiguous provenance when demand evidence links were absent.
  Impact Packaging now fails non-zero for checksum coverage gaps, wrong raw-acquisition-index directory contracts, and demands without supporting evidence IDs; learning-outcome provenance serialization is explicit for unavailable evidence.
  Quality checks pending
  Reviewer @copilot

- 2026-07-15 (fix: enforce protocol-projection constraints and growth-ineligible negative controls)
  Change type fix
  Scope scripts tests governance
  Files affected scripts/export_live_research_records.py tests/test_export_live_research_records.py tests/test_derived_competence_analysis.py CHANGELOG.txt
  Summary Tightened query-constraints fallback so generated protocol projections (`research_queries_from_protocol.yml`) fail non-zero on constraints mismatches instead of silently switching to ad-hoc constraints. Added explicit per-query/provider constraint audit exports (`requested/applied/unsupported/unapplied`) into `query_execution_log.csv`. Added negative regressions proving repeated-only and duplicate-only evidence create no Layer 4 demands and do not contribute Layer 5 credentials, gap rows, or H1/H2/H3 support.
  Reason Remaining PR-191 review blockers required stricter authoritative-constraints enforcement and stronger proof that growth-ineligible evidence cannot produce scientific-growth outputs.
  Impact Live acquisition audit output now distinguishes requested versus applied versus unsupported/unapplied filters per provider/query, and growth-ineligible evidence is contract-tested to stay out of scientific-growth demand/hypothesis paths.
  Quality checks python -m pytest tests/test_export_live_research_records.py tests/test_derived_competence_analysis.py -q; python -m flake8 src scripts tests run_full_analysis.py main.py; python -m mypy src scripts run_full_analysis.py main.py; python -m pytest tests/ -q; python scripts/validate_generated_outputs.py; python scripts/validate_run_archive_integrity.py --archive-root outputs/run_archive --require-present.
  Reviewer @copilot

- 2026-07-15 (fix: tighten growth/query-family gates and Layer 1 axis/count serialization contracts)
  Change type fix
  Scope src scripts tests governance
  Files affected src/scientific_sources/derived_competence_analysis.py src/scientific_sources/live_run_audit.py scripts/compute_live_novelty_metrics.py tests/test_derived_competence_analysis.py tests/test_live_run_audit.py tests/test_compute_live_novelty_metrics.py CHANGELOG.txt
  Summary Layer 4 demand aggregation now enforces growth-eligible novelty statuses for per-demand evidence metrics; learning-outcome prose now emits explicit `evidence=unavailable` when provenance links are absent; H2 partial-support threshold is aligned to the documented 0.25 boundary. Gate E now treats empty `query_families_seen` as unavailable and emits the explicit availability warning. Layer 1 raw acquisition index now serializes canonical axis identity split (`axis_group`, `axis_code`) and prefers exporter query-execution counts for raw/normalized record-count semantics.
  Reason PR #191 follow-up review identified remaining contract gaps around growth filtering clarity, explicit provenance absence signaling, Gate E family-availability semantics, Layer 1 axis serialization, and raw-count semantics.
  Impact Derived-demand and novelty-gate outputs better match scientific validity contracts; Layer 1 acquisition rows carry explicit canonical axis name/code fields and more faithful per-query count semantics from exporter diagnostics.
  Quality checks python -m pytest tests/test_derived_competence_analysis.py tests/test_compute_live_novelty_metrics.py tests/test_live_run_audit.py -q; python -m flake8 src scripts tests run_full_analysis.py main.py; python -m mypy src scripts run_full_analysis.py main.py; python -m pytest tests/ -q.
  Reviewer @copilot

- 2026-07-15 (fix: remove placeholder learning-outcome evidence reference)
  Change type fix
  Scope src tests governance
  Files affected src/scientific_sources/derived_competence_analysis.py tests/test_derived_competence_analysis.py CHANGELOG.txt
  Summary Removed the `see_learning_outcomes_evidence_id` placeholder fallback in Layer 5 learning-outcome prose. When evidence linkage is unavailable, the `evidence=` field is now empty rather than emitting a fabricated placeholder token.
  Reason Follow-up PR feedback identified placeholder evidence references as fabricated provenance in published learning-outcome statements.
  Impact Learning outcomes no longer serialize synthetic placeholder evidence IDs; provenance remains explicit and non-fabricated.
  Quality checks python -m pytest tests/test_derived_competence_analysis.py -v; python -m flake8 src scripts tests run_full_analysis.py main.py; python -m mypy src scripts run_full_analysis.py main.py.
  Reviewer @copilot

- 2026-07-15 (fix: PR-191 review-findings scientific-validity, provenance, packaging, and workflow-contract hardening)
  Change type fix
  Scope scripts src tests config docs workflows
  Files affected .github/workflows/full-live-analysis.yml scripts/build_live_cumulative_release_package.py scripts/build_layer4_5_scientific_analysis.py scripts/build_statistical_research_report.py src/scientific_sources/derived_competence_analysis.py config/live_query_protocol.yml docs/LIVE_CUMULATIVE_SCIENTIFIC_DATABASE.md tests/test_full_live_analysis_workflow.py tests/test_live_cumulative_release_package.py tests/test_workflow_security.py CHANGELOG.txt
  Summary Addresses all blocking review findings from PR-191: (1) pass generated constraints path explicitly to acquisition exporter; (2) release package removes stale ZIP, validates manifest/checksum/CSV schemas before packaging; (3) ZipInfo entries use ZIP_DEFLATED compress_type; (4) run-id path validation uses exact path-segment matching; (5) Layer 4 demand aggregation filters signals by growth-eligible evidence IDs; (6) micro_credential_coverage_index reports not_computable without validated supply map; (7) QMBD skewness initializes all 4 canonical axes; (8) removes fabricated evidence fallback; (9) Cohen's d reports not_computable on zero pooled SD; (10) rejects EQF levels above 7; (11) adds versioned hypothesis declarations to protocol; (12) missing hypothesis outputs are structural defects; (13) fixes stale scoring formula and metadata_only description in docs; (14) credential coverage distinguishes validated_partial from validated_covered; (15) workflow security test evaluates effective permissions (workflow+job scopes); (16) layer readiness report validates file content schemas.
  Reason Reviewer identified multiple scientific-validity, provenance-integrity, packaging-correctness, and security-contract defects that required resolution before merge.
  Impact All review blocking findings resolved; scientific indices, hypothesis verification, credential coverage, and release packaging now enforce correct contracts.
  Quality checks python -m flake8 src scripts tests run_full_analysis.py main.py; python -m mypy src scripts run_full_analysis.py main.py; python -m pytest tests/ -v (1026 passed); python scripts/validate_generated_outputs.py; python scripts/validate_run_archive_integrity.py --archive-root outputs/run_archive --require-present.
  Reviewer @copilot

- 2026-07-14 (fix: PR-191 deterministic Layer 4-5 recency timestamp + Gate A provider-alias canonicalisation)
  Change type fix
  Scope workflows scripts tests governance
  Files affected .github/workflows/full-live-analysis.yml scripts/compute_live_novelty_metrics.py tests/test_full_live_analysis_workflow.py tests/test_compute_live_novelty_metrics.py CHANGELOG.txt
  Summary Full-live workflow now captures a single job-level `ANALYSIS_TIMESTAMP_UTC` via one `date -u` call and passes it to the Layer 4-5 build as `--analysis-timestamp-utc`, so recency-sensitive Layer 4-5 outputs are deterministic within a run and do not drift across steps. Gate A in `compute_live_novelty_metrics.py` now canonicalises provider aliases on both the provider-health and contribution-count sides through a shared `_canonical_provider` map (reconciling `wos`, `Web of Science`, `Web of Science (Clarivate)`, `Clarivate WoS`, and variants) and accumulates counts across alias labels, so a healthy WoS provider with contributed records is not falsely flagged as zero-contribution while still failing correctly when it truly returned zero records.
  Reason Reviewer identified that Layer 4-5 recency published values could vary within a single run because independent steps otherwise call wall-clock `datetime.now()`, and that Gate A could raise false zero-contribution failures when provider labels differed between the health report and the contribution counts.
  Impact Layer 4-5 published recency values are reproducible for a given run; two identical Full Live-Enriched Analysis runs can be compared without wall-clock drift. Gate A no longer produces false positives or false negatives caused solely by provider label variation between the health preflight and the Layer 2-3 contribution counts.
  Quality checks python -m pytest tests/test_full_live_analysis_workflow.py tests/test_compute_live_novelty_metrics.py -v; python -m flake8 src scripts tests run_full_analysis.py main.py; python -m mypy src scripts run_full_analysis.py main.py.
  Reviewer @copilot

- 2026-07-13 (fix: PR-191 semantic-enrichment guard + executable package invocation + taxonomy/provenance hardening)
  Change type fix
  Scope src workflows tests governance
  Files affected src/scientific_sources/cumulative_scientific_database.py src/scientific_sources/derived_competence_analysis.py .github/workflows/full-live-analysis.yml tests/test_full_live_analysis_workflow.py tests/test_derived_competence_analysis.py CHANGELOG.txt
  Summary Removed early novelty-stage semantic-enrichment upgrades from `_classify_novelty`/`_upgrade_if_enriched` so repeated records remain `repeated_record` until stable signal-ID reconciliation confirms genuinely new cross-run semantic identities. Updated full-live package invocation to pass required `--stats-dir` and Layer 1 `--raw-acquisition-index` arguments. Layer 4 taxonomy now consumes duplicate-filtered signals and emits explicit `primary_axis`, `secondary_axes`, `axis_bridge_score`, and `matched_hypothesis_ids` metadata; Layer 5 now records nuanced `coverage_status` values (`candidate_translation`, `review_required`, `validated_covered`, `validated_uncovered`) and carries demand evidence IDs into generated learning-outcome prose.
  Reason Review feedback identified current-head blockers across recurrence classification, package invocation, taxonomy traceability, and credential/outcome provenance semantics.
  Impact Repeated identical signals cannot be upgraded by pre-reconciliation novelty classification; full-live package creation now receives the required Layer 1 and Layer 4 inputs; taxonomy output preserves canonical axis names/codes plus bridge metadata while excluding duplicate-only evidence; credential coverage reporting no longer collapses to binary covered/uncovered labels and outcomes include explicit demand evidence references.
  Quality checks python -m pytest tests/test_cumulative_scientific_database.py tests/test_full_live_analysis_workflow.py -v; python -m pytest tests/test_derived_competence_analysis.py tests/test_full_live_analysis_workflow.py tests/test_cumulative_scientific_database.py -v; python -m pytest tests/test_derived_competence_analysis.py -v; python -m flake8 src scripts tests run_full_analysis.py main.py; python -m mypy src scripts run_full_analysis.py main.py.
  Reviewer @copilot

- 2026-07-13 (fix: scope canonical flake8 targets to source tree)
  Change type fix
  Scope workflows tests docs governance
  Files affected .github/workflows/ci.yml tests/test_ci_workflow.py CONTRIBUTING.md .github/copilot-instructions.md CHANGELOG.txt
  Summary Updated the canonical flake8 invocation from repository-root (`python -m flake8 .`) to explicit source-tree targets (`python -m flake8 src scripts tests run_full_analysis.py main.py`) to prevent runner-local `.git/logs/.../*.py` ref-log filenames from being linted.
  Reason Local runs can fail non-deterministically when fetched ref-log names under `.git/logs` end with `.py`; the scoped target list keeps linting focused on tracked Python source files.
  Impact Static-quality CI, contributor workflow guidance, and CI workflow assertions now align on deterministic source-tree lint scope.
  Quality checks python -m flake8 src scripts tests run_full_analysis.py main.py; python -m mypy src scripts run_full_analysis.py main.py; python -m pytest tests/test_ci_workflow.py -v; python scripts/validate_generated_outputs.py; python scripts/validate_run_archive_integrity.py --archive-root outputs/run_archive --require-present.
  Reviewer @copilot

- 2026-07-13 (fix: PR-191 pass — Layer 1 axis decode + hard novelty gate exits)
  Change type fix
  Scope src scripts tests workflows governance
  Files affected src/scientific_sources/cumulative_scientific_database.py scripts/compute_live_novelty_metrics.py .github/workflows/full-live-analysis.yml tests/test_cumulative_scientific_database.py tests/test_compute_live_novelty_metrics.py tests/test_full_live_analysis_workflow.py CHANGELOG.txt
  Summary Fixed Layer 1->Layer 2 axis identity decoding for live-path audit rows that serialize axis values (`M/T/O/H`) by decoding axis_target/axis_code into canonical axis_group + display axis_code during binding. Hardened novelty gate CLI failure contract so any Gate fail returns non-zero, and updated full-live-analysis workflow to run novelty evaluation with `--strict`.
  Reason PR #191 review comment 4962953678 reported two remaining blockers at head 82d6065: broken axis identity decoding in `_load_layer1_bindings` for value-form axis_target, and novelty gate failures exiting zero without strict workflow invocation.
  Impact Layer 2 evidence rows now keep correct axis candidates when Layer 1 audit contains value-form axis identifiers, and hard novelty failures (including Gate D and consecutive-zero Gate B) now fail CI deterministically.
  Quality checks Targeted pytest suites passed (TestLayer1Binding, test_compute_live_novelty_metrics, test_full_live_analysis_workflow). Full pytest passed (1001 passed, 5 skipped). mypy passed. validate_generated_outputs and validate_run_archive_integrity passed. `python -m flake8 .` failed locally due fetched `.git/logs/.../*.py` ref-log filenames; canonical source-tree flake8 (`python -m flake8 src scripts tests run_full_analysis.py main.py`) passed.
  Reviewer @copilot

- 2026-07-13 (fix: PR-191 scientific-validity hardening — 8 evidence-boundary and pipeline correctness fixes)
  Change type fix
  Scope src scripts tests workflows
  Files affected src/scientific_sources/cumulative_scientific_database.py src/scientific_sources/derived_competence_analysis.py src/scientific_sources/live_query_protocol.py scripts/build_live_cumulative_release_package.py scripts/export_live_query_protocol_projection.py .github/workflows/full-live-analysis.yml tests/test_cumulative_scientific_database.py tests/test_derived_competence_analysis.py tests/test_live_cumulative_release_package.py CHANGELOG.txt MANIFEST_SOURCES.csv
  Summary Eight scientific-validity and pipeline-correctness fixes: (1) protocol projection now exports per-query time_window/sort_strategy/sampling_strategy constraints for Layer 1 audit; (2) duplicate_only evidence excluded from demand aggregation, demand-strength numerators, gap evidence, credential translation, hypothesis fragments, and hypothesis statistics; (3) H2 corrected to compute covered/missing demand IDs (not credential-row counts) and flags candidate-translation coverage explicitly; (4) release package fails non-zero before ZIP when any required Layer 0-5 artifact is missing; (5) DOI canonicalization strips URL prefixes (https://doi.org/, http://doi.org/, https://dx.doi.org/, doi:) and URL-decodes payloads so bare and URL-form DOIs deduplicate to one evidence ID; (6) year/journal/citation_count metadata prefers the latest non-empty enriched value rather than the first (sparse) observation; (7) Jaccard near-duplicate non-root title buckets are marked duplicate_only to prevent growth-metric and demand-score inflation, with jaccard_group_id retained as full crosswalk; (8) environment: live-research added to full-live-analysis.yml so proprietary-secret jobs require a gated environment before running.
  Reason PR-191 review comment #4960594945 listed 8 required fixes with negative regression tests before merge.
  Impact DOI-form records that previously duplicated the corpus now correctly merge; demand-strength scores are no longer inflated by duplicate_only evidence; H2 Hydronization Lag metric is now per competence_demand_id as specified; release package cannot be built without all Layer 0-5 tables; live acquisition is gated behind the live-research environment; protocol constraints are traceable in the Layer 1 audit bundle.
  Quality checks flake8 clean; mypy clean (no issues in 5 source files); 82 focused pytest tests pass (including 10 new regression tests).
  Reviewer @copilot

- 2026-07-10 (fix: PR-190 review follow-up — clarify Layer 0-5 scaffold scope, evidence limits, and package determinism)
  Change type fix
  Scope src scripts tests workflows docs governance
  Files affected README.md docs/LIVE_CUMULATIVE_SCIENTIFIC_DATABASE.md docs/STATISTICAL_REPORT_METHODOLOGY.md src/scientific_sources/derived_competence_analysis.py scripts/build_live_cumulative_release_package.py tests/test_derived_competence_analysis.py tests/test_live_cumulative_release_package.py CHANGELOG.txt
  Summary Addressed the two latest PR review comments by correcting the public scope from Layer 0-1 / Layer 2-3-only wording to a PR-190 Stage A-C full-pipeline scaffold, while explicitly warning that credential coverage and hypothesis inference remain preliminary until validated coverage and richer statistical models are implemented. Documented that raw_api_payloads are exporter-level acquisition envelopes, semantic competence-demand signals are metadata-derived unless abstracts/full text are present, and query intent is not scientific evidence. Added a package --generated-at-utc option so README/citation/manifest timestamps can be frozen for byte-identical ZIP rebuilds, and changed generated learning outcomes from a generic Apply template to EQF-aware Bloom-style statements carrying evidence, demand, and confidence traceability.
  Reason Reviewers noted that the PR body and docs were stale after Layers 2-5, novelty gates, report, and release package were added; they also requested documentation of raw payload provenance, metadata-derived signal limitations, preliminary hypothesis status, generated-vs-validated credential coverage, and deterministic package timestamp control.
  Impact Reviewers can now evaluate the current branch as a coherent Stage A-C scaffold without mistaking it for final scientific-statistical validation. Release package determinism can be tested with a frozen generated_at timestamp, and candidate learning outcomes expose stronger audit traces back to Layer 2-4 evidence.
  Quality checks flake8 clean; mypy clean (Success: no issues found in 63 source files); focused pytest for derived competence analysis, live cumulative release package, and full-live workflow passed 20/20; full pytest passed 960/960 with 5 skipped and 8 openpyxl warnings; validate_generated_outputs.py PASSED; validate_run_archive_integrity.py --archive-root outputs/run_archive --require-present PASSED for 2 archived runs.
  Reviewer @codex

- 2026-07-09 (feat: PR-190 Layers 2-3 — live cumulative scientific database with deduplication, novelty classification, and deterministic semantic competence-demand signals)
  Change type feat
  Scope src scripts tests workflows docs governance outputs
  Files affected src/scientific_sources/cumulative_scientific_database.py scripts/build_cumulative_scientific_database.py tests/test_cumulative_scientific_database.py tests/test_full_live_analysis_workflow.py .github/workflows/full-live-analysis.yml docs/LIVE_CUMULATIVE_SCIENTIFIC_DATABASE.md MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Added a purely additive Layer 2-3 builder that materializes a live cumulative scientific database under outputs/cumulative_database/. Layer 2 (evidence records) reads Layer 1 raw-acquisition bundles under outputs/live_runs/<run_id>/raw/raw_acquisition_index.csv, the run archive at outputs/run_archive/cumulative_runs_index.csv (+ per-run outputs/run_archive/runs/<run_id>/research_sources/live_records.json), and the current run's outputs/research_sources/live_records.json, then dedupes rows in strict priority order (canonical DOI → normalized-title SHA-256 prefix → provider source_id), computes first_seen_run_id / latest_seen_run_id, groups near-duplicate titles via a Jaccard union-find (threshold 0.85), and classifies every evidence row into one of {new_record, repeated_record, updated_metadata, provider_enriched, semantic_enriched, duplicate_only, review_required}. Layer 3 (semantic competence-demand signals) is a deterministic rule-based scanner (no LLM, no dynamic code) that walks title + subject_terms + source_query text using 13 hard-coded phrase groups covering explicit_competence_demand, workforce_skill, education_training_signal, learning_outcome_signal, credential_translation_signal, digital_skill, technical_skill, governance_skill, policy_regulation_skill, social_science_skill, sustainability_skill, safety_risk_skill, and implicit_competence_demand. Every signal row records evidence_id, run_id, sector, axis_group, axis_code, query_id, query_family, semantic_scope, signal_type, competence_label, competence_description, demand_phrase, learning_outcome_candidate, evidence_text_scope, evidence_text_hash, confidence_score, classifier_version (cumulative-db-semantic-v1), manual_review_status (auto_accepted|review_required), and validity_warning. Confidence scoring is deterministic: +0.55 for a title hit, +0.20 for a subject hit, +0.15 for a query hit, −0.10 for metadata-only evidence, clamped to [0.05, 0.95], with the 0.50 threshold gating auto_accepted vs review_required. Records whose signals emit are reconciled by upgrading repeated_record → semantic_enriched. The bundle is 8 byte-identical files: evidence_records.{csv,jsonl} (20 columns each), competence_demand_signals.{csv,jsonl} (20 columns each), run_novelty_metrics.{csv,json} (12+ fields including new_unique_doi_count, repeated_doi_count, updated_metadata_count, provider_enriched_count, semantic_new_signal_count, provider_record_count_by_provider, provider_health_ok_zero_records, jaccard_similarity_with_previous_run, provider_diversity_score, query_diversity_score, crossref_dominance_ratio, validity_warnings), database_manifest.json (schema_version 1.0.0 + per-file sha256 + allowed-vocabulary catalog + workflow_context), and _checksums.sha256 (deterministic <sha256>  <relpath> lines including the manifest itself). All JSON is written with sort_keys=True and ensure_ascii=False; all CSV rows are written with lineterminator="\n" and pre-sorted by stable keys. The builder accepts a --built-at-utc override so rebuilds are byte-identical across runs. Added scripts/build_cumulative_scientific_database.py as a thin CLI wrapper with --archive-root, --live-runs-root, --current-run, --query-protocol, --output-dir, --current-run-id, --built-at-utc, and --emit-summary; missing optional inputs (archive_root, live_runs_root, protocol) trigger a stderr warning but never fail the run. Integrated the builder into .github/workflows/full-live-analysis.yml as a new step "Build cumulative scientific database (Layers 2 and 3)" positioned strictly after "Validate archived run integrity" so Layer 2-3 can only run against integrity-verified archives; added outputs/cumulative_database/ to the artifact upload path and to the git add invocation in the commit-outputs job. Added tests/test_cumulative_scientific_database.py with 26 tests covering bundle shape and vocabulary discipline, dedupe order (DOI, title, source_id, distinct DOIs, source_id-only → review_required), novelty classification (new_record on first appearance, repeated_record for archive-only, provider_enriched when current adds a provider, repeated records not counted in new_unique_doi_count), Layer 1 protocol_binding=bound query_id propagation, unbound rows leaving Layer 0 fields empty, semantic signal extraction (explicit competence demand emits, unrelated records emit nothing, weak evidence yields review_required, abstract-present suppresses the metadata_only warning, all signal_types stay inside ALLOWED_SIGNAL_TYPES, repeated_record → semantic_enriched upgrade when signals fire), byte-identical determinism across two builds, CSV column-order stability, novelty-metric completeness, jaccard=0 when there is no previous run, graceful behavior when archive_root/protocol are absent, current_run_no_records warning, and CLI --help. Extended tests/test_full_live_analysis_workflow.py with four new static-workflow assertions: (1) the workflow calls scripts/build_cumulative_scientific_database.py with the exact --current-run, --archive-root, --live-runs-root, --query-protocol, --output-dir, and --current-run-id arguments; (2) the Layer 2-3 step runs strictly after "Validate archived run integrity"; (3) outputs/cumulative_database/ is present in the artifact upload block; (4) outputs/cumulative_database/ is staged by the commit-outputs job's git add. Added docs/LIVE_CUMULATIVE_SCIENTIFIC_DATABASE.md explaining bundle layout, allowed vocabularies, confidence scoring, and CLI usage. Layer 0-1 (config/live_query_protocol.yml, src/scientific_sources/live_query_protocol.py, src/scientific_sources/live_run_audit.py, scripts/build_live_run_audit.py) is UNCHANGED and NOT weakened; the exporter contract, run archive layout, sync-gate hardening, deterministic output generation, and manifest/checksum discipline from PR-186/187/189 remain UNCHANGED; Layer 4-5, novelty gates, report, and release ZIP were later added in PR-190 Stage C; this Layer 2-3 entry is retained as historical context for the Task B commit only.
  Reason PR-190 Task B requires a live cumulative scientific database that (a) survives across runs by binding into the existing run archive and Layer 1 raw-acquisition bundles, (b) does not fabricate abstracts or citations, (c) uses a deterministic and inspectable classifier for semantic signals so reviewers can audit every learning-outcome candidate, and (d) publishes per-run novelty metrics so downstream statistical analysis can distinguish first-appearance evidence from provider re-observation and semantic re-observation. Layers 2-3 deliver this substrate; Layer 4-5 will consume it in follow-up PRs.
  Impact New outputs/cumulative_database/ bundle is available for consumption by follow-up PRs implementing Layers 4-5; the Layer 0-1 contract, exporter contract, run archive layout, sync-gate hardening, and manifest/checksum discipline are all unaffected; strict-mode reviewers can audit dedupe-key selection, first/latest_seen provenance, semantic-signal confidence scoring, and cross-run novelty computation from the bundle alone.
  Provenance Bundle is strictly downstream of outputs/research_sources/ (unchanged), outputs/run_archive/ (unchanged), and outputs/live_runs/ (unchanged); Layer 0 registry at config/live_query_protocol.yml is the fallback protocol_binding source when Layer 1 does not resolve a query text.
  Quality checks flake8 clean on the whole repo; mypy clean (Success: no issues found in 58 source files) with --python_version=3.10; pytest 950/950 pass (13/13 tests/test_full_live_analysis_workflow.py including 4 new Layer 2-3 assertions; 26/26 tests/test_cumulative_scientific_database.py); scripts/validate_generated_outputs.py PASSED; smoke run against real repository data produced 1592 evidence rows + 2257 semantic signals with byte-identical rebuild (diff -r ran with 0 differences); scripts/generate_manifest.py regenerated deterministically.
  Reviewer @robertbartlomiejski

- 2026-07-09 (feat: PR-190 Layer 0-1 integration — wire live-run audit into full-live-analysis workflow and stabilize mypy)
  Change type feat
  Scope scripts workflows tests governance
  Files affected .github/workflows/full-live-analysis.yml src/scientific_sources/live_query_protocol.py tests/test_full_live_analysis_workflow.py CHANGELOG.txt
  Summary Integrated the Layer 1 audit builder into the Full Live-Enriched Analysis workflow: added a new step "Build Layer 1 live-run audit bundle" that runs scripts/build_live_run_audit.py with --run-id "${{ github.run_id }}-${{ github.run_attempt }}" (matching the archive_run_outputs run_id convention exactly), --research-sources-dir outputs/research_sources, --output-root outputs/live_runs, and --protocol-path config/live_query_protocol.yml, positioned after the research-source validation gate and before run_full_analysis so raw acquisition is preserved before any downstream normalization. Added outputs/live_runs/ to the paths of the "Upload live-enriched analysis outputs" artifact so reviewers can inspect per-run bundles for every workflow_dispatch and scheduled run, and to the git add invocation in the commit-outputs job so bot commits stage the bundles when vars.ALLOW_BOT_COMMITS=true. Fixed a stale mypy blocker at src/scientific_sources/live_query_protocol.py:29 by annotating the yaml import with # type: ignore[import-untyped], mirroring the canonical repo pattern already used in scripts/export_live_research_records.py:42 — this preserves full validation (no --ignore-missing-imports, no relaxation of warn_return_any) without adding types-PyYAML to dev dependencies. Extended tests/test_full_live_analysis_workflow.py with four new static-workflow assertions: (1) the workflow calls scripts/build_live_run_audit.py with the exact --research-sources-dir, --output-root, and --protocol-path arguments; (2) the run_id passed to build_live_run_audit.py matches the archive convention "${{ github.run_id }}-${{ github.run_attempt }}"; (3) outputs/live_runs/ is present in the artifact upload block; (4) outputs/live_runs/ is staged by the commit-outputs job's git add. The exporter contract, run archive layout, sync-gate hardening, and manifest/checksum discipline from PR-186/187/189 remain UNCHANGED; Layer 2-5 have NOT been started (deferred to follow-up PRs).
  Reason PR-190 Layer 1 code (src/scientific_sources/live_run_audit.py, scripts/build_live_run_audit.py, tests/test_live_run_audit.py, config/live_query_protocol.yml, src/scientific_sources/live_query_protocol.py) shipped in commit 9aa026f but was not yet exercised by CI, and a residual mypy import-untyped error prevented `python -m mypy src scripts run_full_analysis.py main.py` from succeeding. This integration ensures every future live-enriched run publishes a raw-provider audit bundle alongside normalized outputs, and re-establishes a clean mypy baseline before Layer 2-5 work begins.
  Impact The Full Live-Enriched Analysis workflow now materializes outputs/live_runs/<run_id>/ on every dispatch or scheduled run and preserves it in the analysis artifact and (optionally) in bot-committed refresh cycles; downstream layers (novelty gates, cumulative statistical database, statistical research report) can now consume the raw-vs-normalized descent record per run; mypy baseline is restored to Success: no issues found in 56 source files.
  Provenance Workflow additions are strictly downstream of outputs/research_sources/ (unchanged), reuse the archive_run_outputs run_id convention (unchanged), and bind acquisition to config/live_query_protocol.yml (unchanged since PR-190 Layer 0). mypy fix follows the yaml-import pattern in scripts/export_live_research_records.py:42.
  Quality checks flake8 clean on src scripts tests run_full_analysis.py main.py; mypy clean (Success: no issues found in 56 source files); pytest 920/920 pass (9/9 tests/test_full_live_analysis_workflow.py including 4 new Layer 1 assertions); scripts/validate_generated_outputs.py PASSED; scripts/validate_run_archive_integrity.py --require-present PASSED for 2 archived runs; scripts/generate_manifest.py regenerated deterministically (1167 entries, no delta from prior state).
  Reviewer @robertbartlomiejski

- 2026-07-09 (feat: PR-190 Layer 1 — raw provider acquisition audit bundle with deterministic manifest and Layer 0 protocol binding)
  Change type feat
  Scope src scripts tests governance outputs
  Files affected src/scientific_sources/live_run_audit.py scripts/build_live_run_audit.py tests/test_live_run_audit.py MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Added a purely additive Layer 1 raw-provider-acquisition audit builder that constructs a deterministic per-run bundle under outputs/live_runs/<run_id>/ preserving raw provider acquisition separately from normalized evidence. The bundle contains live_run_manifest.json (schema_version 1.0.0, run_id, built_at_utc, per-family/per-provider counts, workflow_context, per-file sha256), _checksums.sha256 (deterministic <sha256>  <relpath> lines including the manifest itself), raw/raw_acquisition_index.csv (15 stable columns per (query_id, provider) row: query_id, sector_slug, sector_label, axis_target, query_family, provider, query_text, raw_record_count, normalized_record_count, unique_source_ids, coverage_record_count, has_raw_payload_envelope, raw_payload_sha256, raw_payload_captured_at, protocol_binding), raw/raw_provider_records.json (copy of pre-merge raw provider rows), raw/raw_api_payloads/ (copy of per-query envelopes emitted by the exporter, when present), and normalized/{live_records.json, live_provenance.json, live_source_coverage.csv} (copies of the normalized/triangulated winners). The builder maps every observed source_query back to its Layer 0 query_id / sector_slug / query_family / axis_target via case-insensitive query_text lookup, marking each row protocol_binding=bound|unbound|no_protocol. Every output is deterministic: JSON is written with sort_keys=True and 2-space indent; CSV rows are sorted by (sector_slug, query_id, provider); the manifest accepts an optional built_at_utc override so byte-identical rebuilds are possible; _safe_run_id() rejects ../, /, hidden dirs, whitespace, and empty strings before touching the filesystem. Added a CLI wrapper scripts/build_live_run_audit.py that collects GITHUB_* env vars into workflow_context, warns (never fails) when the protocol registry is missing, and optionally emits a one-line JSON summary. Added tests/test_live_run_audit.py with 34 test cases covering bundle shape, per-column stability, protocol-bound vs unbound vs no_protocol rows, evidence-only queries, checksum coverage of the manifest, byte-identical rebuilds with frozen timestamps, prior-bundle replacement on rebuild, malformed-input rejection (missing dir, missing raw records, non-list, invalid JSON, non-object row, malformed envelope), 7 parametrized unsafe run_id rejections, envelope metadata surfacing, workflow_context embedding, string-path dataclass acceptance, shipped-protocol binding of synthetic queries, source-file non-mutation, CLI main() import/exec/exit-code behavior, non-touching-other-run-directories, and public-surface docstring completeness. The exporter scripts/export_live_research_records.py, the run-archive layout, and the manifest/checksum discipline from PR-186/187/189 are UNCHANGED — Layer 1 is a strictly downstream additive builder.
  Reason PR-190 requires that raw provider acquisition be preserved separately from normalized evidence so that downstream layers (novelty gates, cumulative statistical database, statistical research report) can prove every normalized record descends from a raw acquisition observed during the same run. Layer 1 delivers this separation at the directory level, binds raw acquisition to the Layer 0 protocol registry, and does so without weakening any existing safeguard.
  Impact New outputs/live_runs/<run_id>/ bundle is available for consumption by follow-up PRs implementing Layers 2-5; exporter contract, run archive layout, sync-gate hardening, deterministic output generation, and manifest/checksum discipline are all unaffected; strict-mode reviewers can audit raw-vs-normalized descent per run.
  Provenance Bundle layout is additive to outputs/research_sources/ (unchanged) and outputs/run_archive/ (unchanged); Layer 0 registry at config/live_query_protocol.yml is the sole binding source for query_id / axis_target / query_family / sector_slug on acquisition rows.
  Quality checks 34/34 new tests pass (tests/test_live_run_audit.py); full pytest sweep passes (880 total); flake8 clean on new files; mypy clean on new files (Python 3.12); secret scan clean; changelog_guard passes; MANIFEST_SOURCES.csv regenerated deterministically (+3 entries for the new files); byte-identical bundle rebuild verified via diff -qr with frozen built_at_utc.
  Reviewer @robertbartlomiejski

- 2026-07-08 (feat: PR-190 Layer 0 — live query protocol registry with typed loader and legacy-view backward-compat)
  Change type feat
  Scope config scripts tests governance
  Files affected config/live_query_protocol.yml src/scientific_sources/live_query_protocol.py tests/test_live_query_protocol.py MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Introduced config/live_query_protocol.yml as the structured successor to config/research_queries.yml. The new registry declares 12 blue economy sectors × 8 queries each (96 total, all unique query_ids) across 4 query families (core_sector, competence_demand, emerging_demand, validation_eqf_translation), satisfying the PR-190 per-sector minimums (≥3 core, ≥2 competence, ≥2 emerging, ≥1 validation). Each query record carries axis_target (BlueDynamicsAxis), evidence_intent, time_window (from_year/to_year), sort_strategy (crossref/scopus/wos), sampling_strategy (mode/pages/rows_per_page/dedupe_key), and expected_signal. Added typed loader src/scientific_sources/live_query_protocol.py with frozen dataclasses (TimeWindow, SortStrategy, SamplingStrategy, LiveQuery, LiveQuerySector, LiveQueryProtocol), strict schema/minimum/uniqueness validation via LiveQueryProtocolError, and a to_legacy_query_groups() shim that renders the protocol into the exact shape consumed by scripts/export_live_research_records.py. The legacy config/research_queries.yml is UNCHANGED and remains the default input to the exporter; the new protocol is additive-only for this PR. All 36 legacy query strings are preserved verbatim in the core_sector family. Added tests/test_live_query_protocol.py with 36 test cases covering structural conformance of the shipped file, per-sector family minimums, workforce/emerging/EQF vocabulary presence, legacy-view slug/label/query parity, and 17 loader validation rejection scenarios.
  Reason PR-190 requires a structured live query protocol registry with per-family minimums as Layer 0 of the live cumulative scientific database. The registry provides the vocabulary and sampling contract for downstream layers (novelty gates, cumulative DB, statistical report) without weakening any PR-186/187/189 safeguards.
  Impact New protocol registry is available for consumption by follow-up PRs implementing Layers 1-5; legacy exporter contract is preserved verbatim via the backward-compat shim; run_full_analysis default execution, archive integrity validation, deterministic output generation, and manifest/checksum discipline are unaffected.
  Provenance Registry sector labels are pinned to run_full_analysis.SECTORS (12 canonical blue economy sectors); axis assignments use src.core.BlueDynamicsAxis (MARINE/MARITIME/OCEANIC/HYDRONIZATION); competence and emerging vocabularies follow the PR-190 specification.
  Quality checks 36/36 new tests pass (tests/test_live_query_protocol.py); MANIFEST_SOURCES.csv regenerated deterministically (1164 entries; +3 for the new files); config/research_queries.yml unchanged.

- 2026-07-13 (governance: align Copilot cloud agent with four-axis scientific pipeline)
  Change type update add
  Scope governance workflows documentation
  Files affected .github/copilot-instructions.md .github/competence-domain.instructions.md .github/instructions/data-pipeline.instructions.md .github/workflows/copilot-setup-steps.yml docs/COPILOT_CLOUD_AGENT_SETUP.md .gitignore requirements.txt MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Replaced stale three-axis agent context with the canonical four-axis name/code contract, added Layer 0-5 evidence and structural validity rules, created a least-privilege Python 3.11 Copilot setup workflow with pinned validation tools and type stubs, documented cloud-agent/MCP/secret separation, and excluded browser authentication state.
  Reason Prevent agent instructions, query intent, fixtures, credentials or browser sessions from being mistaken for scientific evidence or safe live-provider execution.
  Impact Agent code generation is separated from controlled live API acquisition; no scientific outputs or empirical records are changed.
  Quality checks At commit 084a2a988a19c6515ddc5de993337b879e29e87d, GitHub Actions Copilot Setup Steps #7 PASSED (`python -m pip install --disable-pip-version-check -r requirements.txt`, `python -m pip check`, pinned-version verification, and scientific dependency imports); CI #547 PASSED; Full Analysis #449 PASSED. No local scientific test run or live-provider acquisition was claimed for this governance change.
  Reviewer @robertbartlomiejski

- 2026-07-08 (feat: PR-187 publication step — rebuild cumulative cross-run evidence index and versioned data package v1.0.0 for both runs)
  Change type feat
  Scope outputs governance archive release
  Files affected outputs/run_archive/cross_run_evidence_index.csv outputs/run_archive/cross_run_evidence_occurrences.csv outputs/run_archive/cross_run_run_summary.csv outputs/run_archive/cross_run_evidence_build_report.json outputs/release_packages/morskamary_cumulative_evidence_v1.0.0/* outputs/release_packages/morskamary_cumulative_evidence_v1.0.0.zip MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Rebuilt cross-run cumulative evidence index to incorporate both archived runs (28625632237-1 and 28967267944.2); occurrences_total now 10484 (5242 per run × 2), dedupe_groups_total 2052. Rebuilt versioned research data package v1.0.0 from the updated index: evidence_occurrences.csv 10484 rows, runs.csv 2 rows, schema_validated_rows 12614. RELEASE_MANIFEST.json source_commit_sha updated to bda107a9 (current HEAD). MANIFEST_SOURCES.csv regenerated deterministically (1161 entries). validate_run_archive_integrity PASSED for 2 runs; validate_generated_outputs PASSED.
  Reason The previous analysis commit (cf83d78) archived run 28967267944.2 and updated cumulative_runs_index.csv but did not rebuild the cross-run evidence index or release package to incorporate the second run. The publication/release-data step requires both runs to be reflected in the cumulative evidence tables and downloadable package.
  Impact Cumulative cross-run evidence now spans 2 runs and 10484 occurrences; release package v1.0.0 is publication-ready with complete provenance; all sync-gate hardening from PR-189 preserved.
  Quality checks validate_run_archive_integrity PASSED (2 runs); validate_generated_outputs PASSED; sync_audit --strict ALL CHECKS PASSED; MANIFEST_SOURCES.csv deterministic (1161 entries).
  Reviewer @robertbartlomiejski

- 2026-07-08 (feat: PR-187 merge with PR-189 hardened main and live-enriched smoke execution)
  Change type feat
  Scope outputs governance analysis archive
  Files affected outputs/cumulative_qmbd_records.json outputs/run_archive/runs/28967267944.2/* outputs/run_archive/_index/runs_index.jsonl outputs/run_archive/cumulative_runs_index.csv outputs/run_archive/cross_run_evidence_index.csv outputs/run_archive/cross_run_evidence_occurrences.csv outputs/run_archive/cross_run_run_summary.csv MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Merged PR-189 hardened main into PR-187 (merge commit dcade8c). Preserves all PR-189 deterministic safeguards: math.fsum, competence_id sort, _STATIC_RECOVERY_REASON_ENV, ALLOW_STATIC_RECOVERY_MODE env guard, cumulative_run_metadata provenance, repo-relative paths in build report, test isolation. Executed full live-enriched analysis: 2052 competences (15 baseline + 2037 literature), 26 dynamic evidence-first credentials, 132 sector pathways. All outputs validated (validate_generated_outputs PASSED). Run 28967267944.2 archived and cross-run evidence index updated. MANIFEST_SOURCES.csv regenerated (1165 entries).
  Reason PR-187 was stacked on pre-PR-189 base. Merge onto hardened main inherits sync-gate hardening, versioned data provenance, and deterministic output generation before publication-step execution.
  Impact All PR-189 hardening preserved; live-enriched outputs regenerated deterministically; run archived and indexed; PR-187 branch now forward-compatible with main via merge commit.
  Quality checks 846 tests pass; validate_generated_outputs PASSED; analysis exit code 0; MANIFEST_SOURCES.csv deterministic (1165 entries).
  Reviewer @robertbartlomiejski

- 2026-07-08 (fix: use math.fsum for deterministic Average_Confidence across Python versions)
  Change type fix
  Scope scripts tests
  Files affected run_full_analysis.py tests/test_gap_model.py
  Summary Replaced sum() with math.fsum() in export_gap_priority_ranking_csv() for computing Average_Confidence. Python 3.11's sum() on generator expressions accumulates floats with slightly less precision than Python 3.12, causing the Infra & Robotics / MARITIME cluster (20×0.75 + 28×0.90 = 40.2/48 = 0.8375) to round to "0.837" in CI (Python 3.11) vs "0.838" locally (Python 3.12). math.fsum() uses the Shewchuk compensated-summation algorithm which always returns the nearest IEEE 754 double to the true mathematical sum, giving "0.838" consistently on both versions. Added regression test test_gap_priority_ranking_avg_confidence_midpoint_rounds_up that asserts the exact "0.838" result for this cluster shape.
  Reason full-analysis.yml sync check failed because gap_priority_ranking.csv row 17 Average_Confidence differed between committed output (0.838, Python 3.12) and CI-generated output (0.837, Python 3.11). Committed CSV is already correct and unchanged.
  Impact gap_priority_ranking.csv Average_Confidence is now reproducible across Python 3.11 and 3.12. No change to committed CSV files.
  Quality checks: CI #525 and Full Analysis #427 passed on current PR head cfdc723f6bff360b2d81ac1ca21c1b0e361d9f56. Regression test for Average_Confidence midpoint rounding passes.
  Reviewer @robertbartlomiejski

- 2026-07-08 (fix: deterministic gap_priority_ranking.csv aggregation)
  Change type fix
  Scope scripts tests outputs
  Files affected run_full_analysis.py tests/test_gap_model.py outputs/gap_priority_ranking.csv outputs/credentials_dynamic_database.json outputs/sector_qmbd_learning_pathways.json
  Summary Hardened export_gap_priority_ranking_csv() to sort missing_items by competence_id before computing Average_Confidence, removing floating-point summation-order drift between runs. Fixed _top_values() to break frequency ties alphabetically (-count, value) so Top_Origins, Top_Providers, Top_DOIs, and Top_Titles fields are also stable across runs. Added regression test test_gap_priority_ranking_csv_is_deterministic_across_runs that calls the export twice on identical input and asserts identical CSV output and correct Average_Confidence value. Regenerated affected outputs: gap_priority_ranking.csv, credentials_dynamic_database.json, sector_qmbd_learning_pathways.json.
  Reason Full-analysis sync check (git diff --exit-code -- outputs) failed because Infra & Robotics / MARITIME Average_Confidence drifted between 0.838 and 0.837 across CI runs due to nondeterministic item ordering from live API responses. _top_values tie-breaking was also order-dependent and caused text field drift in gap_priority_ranking.csv and related outputs.
  Impact gap_priority_ranking.csv is now publication-stable: Average_Confidence and all text-aggregation fields are deterministic regardless of live-API response ordering.
  Quality checks All 851 tests pass; two new tests pass; git diff --exit-code clean for tracked outputs after regeneration; sync_audit.py --strict reports no errors.
  Reviewer @robertbartlomiejski

- 2026-07-08 (hardening: PR #189 publication-grade provenance and reproducibility patch)
  Change type fix update add
  Scope scripts governance tests schemas outputs
  Files affected scripts/build_cross_run_evidence_index.py scripts/build_versioned_research_data_package.py schemas/research_data_package_manifest.schema.json tests/test_build_cross_run_evidence_index.py tests/test_build_versioned_research_data_package.py tests/test_run_full_analysis.py outputs/run_archive/cross_run_evidence_build_report.json outputs/release_packages/morskamary_cumulative_evidence_v1.0.0/*
  Summary Five hardening changes applied: (1) Removed absolute CI/local paths from cross_run_evidence_build_report.json — build_cross_run_evidence_index.py now stores repo-relative POSIX paths and redacts any paths outside the repository root. (2) Added explicit preflight check to build_versioned_research_data_package.py that fails early with a clear message listing prerequisite commands; manual-source bootstrap only occurs when --bootstrap-empty-manual-sources true is explicitly passed. (3) Split release manifest commit_sha into source_commit_sha (data inputs) and package_commit_sha (pending_until_merge), updated schema, citation template, and RELEASE_MANIFEST.json accordingly. (4) Hardened test_main_blocks_static_mode_without_recovery_override to explicitly unset ALLOW_STATIC_RECOVERY_MODE so CI env vars cannot leak into the test. (5) Added regression tests: absolute-path checks in build report, missing-prerequisite failure, explicit bootstrap, and provenance-field checks.
  Reason Comment 4916215915 on PR #189 requested publication-grade hardening before final merge: no absolute paths in tracked outputs, deterministic preflight, provenance metadata clarity, and protected live-enriched-default policy.
  Impact cross_run_evidence_build_report.json now contains relative paths; RELEASE_MANIFEST.json uses source_commit_sha/package_commit_sha; package builder fails loudly on missing inputs; static mode guard test is CI-safe.
  Quality checks All tests pass; generate_manifest.py and sync_audit.py --strict clean after manifest regeneration.
  Reviewer @robertbartlomiejski

- 2026-07-08 (feat: build versioned research data package v1.0.0)
  Change type add
  Scope outputs scripts governance
  Files affected outputs/release_packages/morskamary_cumulative_evidence_v1.0.0/* outputs/manual_sources/historical_compatibility.csv outputs/manual_sources/manual_sources_index.csv outputs/run_archive/cross_run_*.csv outputs/run_archive/cross_run_evidence_build_report.json
  Summary Generated the versioned cumulative evidence data package (v1.0.0) using build_versioned_research_data_package.py. Prerequisites (cross-run evidence index and manual sources ledger) were bootstrapped first via build_cross_run_evidence_index.py and empty header-only CSVs for manual_sources. Package contains 7,371 schema-validated rows across CSV, JSONL exports with APA citation metadata and SHA-256 checksums.
  Reason User requested compilation of the versioned research data package as part of PR #189 finalisation workflow (comment 4916019719).
  Impact outputs/release_packages/morskamary_cumulative_evidence_v1.0.0/ and .zip are now committed. Manual sources ledger initialised (empty) for future ingestion. Cross-run evidence index populated from one archived run.
  Quality checks build_versioned_research_data_package.py exited 0; schema_validation errors=[]; validated_tables=7 tables.
  Reviewer @robertbartlomiejski

- 2026-07-08 (sync-audit: exclude transient smoke-test artifact from manifest scan and git tracking)
  Change type fix
  Scope governance scripts
  Files affected .gitignore scripts/generate_manifest.py
  Summary Added outputs/research_api_smoke_report.json to .gitignore (transient CI artifact uploaded by research-api-smoke.yml, never committed) and to IGNORED_FILE_BASENAMES in generate_manifest.py so the file is skipped during manifest scans. This prevents the local sync audit from reporting manifest drift when the smoke report exists on the developer's filesystem after a test run.
  Reason Local sync audit (python scripts/sync_audit.py --strict) failed with manifest drift when outputs/research_api_smoke_report.json was present from a local test run. The file was not ignored by git or by generate_manifest.py, causing a spurious extra row in the regenerated manifest.
  Impact sync_audit --strict now passes cleanly on a local checkout even after running the full test suite. No committed outputs change.
  Quality checks python scripts/sync_audit.py --strict; git diff MANIFEST_SOURCES.csv (no diff); python -m pytest tests/ -q (837 passed).

- 2026-07-08 (CI fix: test isolation, quick-mode-gate env, manifest sync, run-analysis exclusion)
  Change type fix
  Scope governance scripts tests ci
  Files affected tests/test_run_full_analysis.py .github/workflows/ci.yml .github/workflows/full-analysis.yml MANIFEST_SOURCES.csv
  Summary Fixed test_main_static_recovery_emits_cumulative_metadata leaking the CI GITHUB_SHA environment variable (added GITHUB_SHA="" to patch.dict). Added ALLOW_STATIC_RECOVERY_MODE="true" to quick-mode-gate env and removed the outputs sync check from that job (static-mode outputs intentionally differ from committed live-enriched outputs; canonical sync is in full-analysis.yml). Excluded outputs/cumulative_qmbd_records.json from the full-analysis.yml sync check because it contains non-reproducible per-run metadata (commit_sha, timestamp_utc, github_run_id). Regenerated MANIFEST_SOURCES.csv removing stale entries for outputs/manual_sources/ and outputs/run_archive/cross_run_* files that are not tracked in git.
  Reason CI failures on PR-189: test-suite (GITHUB_SHA leakage), governance (stale manifest entries), run-analysis (cumulative metadata drift).
  Impact CI checks now correctly isolate per-run metadata; manifest stays deterministic; static-mode gate no longer errors on missing recovery env var.
  Quality checks python -m pytest tests/test_run_full_analysis.py::test_main_static_recovery_emits_cumulative_metadata -xvs; python3 scripts/generate_manifest.py; git diff MANIFEST_SOURCES.csv (verified stale entries removed).

- 2026-07-07 (dynamic-only execution default, manual source ledger, historical output revalidation, gatekeeper reports)
  Change type add update fix
  Scope scripts analysis tests governance outputs
  Files affected run_full_analysis.py scripts/assert_cumulative_live_enriched.py scripts/archive_run_outputs.py scripts/build_cross_run_evidence_index.py scripts/ingest_manual_supporting_sources.py scripts/revalidate_historical_outputs.py scripts/validate_generated_outputs.py scripts/validate_manual_sources_gatekeeper.py scripts/sync_audit.py schemas/run_archive_manifest.schema.json schemas/cross_run_evidence.schema.json schemas/manual_sources_ledger.schema.json schemas/historical_revalidation_report.schema.json schemas/runs.schema.json schemas/source_bundles.schema.json schemas/evidence_records.schema.json schemas/evidence_occurrences.schema.json schemas/evidence_segments.schema.json schemas/coding_assignments.schema.json schemas/reliability_metrics.schema.json schemas/gap_clusters.schema.json schemas/dynamic_credentials.schema.json schemas/data_quality_indicators.schema.json docs/CROSS_RUN_EVIDENCE_CODEBOOK.md docs/CUMULATIVE_DATABASE_METHODOLOGY.md docs/CONTENT_ANALYSIS_PROTOCOL.md docs/STATISTICAL_ANALYSIS_PLAN.md docs/DATA_RELEASE_POLICY.md README.md tests/test_archive_run_outputs.py tests/test_cumulative_evidence_schemas.py tests/test_publication_cumulative_database.py tests/test_research_support_scripts.py tests/test_run_full_analysis.py tests/test_build_cross_run_evidence_index.py tests/test_ingest_manual_supporting_sources.py tests/test_revalidate_historical_outputs.py tests/test_validate_generated_outputs.py tests/test_validate_manual_sources_gatekeeper.py tests/test_validate_run_archive_integrity.py CHANGELOG.txt MANIFEST_SOURCES.csv outputs/cumulative_qmbd_records.json outputs/competences_full_database.json outputs/credentials_database.json outputs/credentials_dynamic_database.json outputs/credentials_generation_rationale.json outputs/gap_priority_ranking.csv outputs/gaps_by_sector.html outputs/credentials_matrix.html outputs/report_index.html outputs/literature_integration.html outputs/sector_pathways.json outputs/gaps_detailed.json outputs/gaps_by_sector_axis.csv outputs/sector_qmbd_learning_pathways.json outputs/sector_dictionaries/*
  Summary Changed normal runtime mode to live-enriched in run_full_analysis.py and blocked static mode unless an explicit local emergency override (`ALLOW_STATIC_RECOVERY_MODE=true`) is set. Added append-only manual source ingestion (`scripts/ingest_manual_supporting_sources.py`) with stable source IDs and checksum tracking, plus historical ZIP/directory revalidation and recoding to canonical cumulative IDs (`scripts/revalidate_historical_outputs.py`). Extended cross-run evidence indexing to include manual ledger records and added dedicated gatekeeper report generation for duplicate IDs, checksum mismatches, compatibility state, and cumulative growth (`scripts/validate_manual_sources_gatekeeper.py`). Hardened `scripts/sync_audit.py` terminal output to ASCII-safe status labels (`[OK]`, `[WARN]`, `[ERROR]`) so Windows cp1250 shells do not fail on Unicode symbols, documented dynamic mode/manual ledger operations in README, standardized CLI entrypoint handling across the new scripts (`main(argv)`, `parse_args(argv)`, `raise SystemExit(main(sys.argv[1:]))`) with subprocess-backed regression tests, added explicit cumulative-ledger/run-archive metadata for analysis mode, static recovery state/reason, provider set, GitHub run ID, commit SHA, and `timestamp_utc`, introduced publication-grade row schemas for runs/source bundles/evidence/coding/reliability/gaps/credentials/data quality, and added methodology, content-analysis, statistical-analysis, and release-policy documents for later Excel/XLSX/SPSS/Statistica/Python/R dataset publication without committing large empirical artifacts in this Code PR.
  Reason Repository operation needed a cumulative dynamic evidence path with manual document uploads and separate validation from live API workflows, while preserving reproducibility and source traceability.
  Impact New manual-source artifacts are produced under outputs/manual_sources and can be integrated into cumulative evidence indexing without destructive merge/simplification. Static-only normal execution is prevented to avoid deterministic regeneration drift in routine runs. Tracked latest-snapshot outputs now include cumulative-ledger metadata so validators, archive manifests, and later cross-run review can distinguish true live-enriched evidence from static recovery snapshots. The release design is now explicit: normalized audit tables remain canonical, while flattened analysis views and dataset packages serve Excel, Statistica, PS IMAGO/SPSS, Python, and R workflows.
  Provenance User-requested follow-up PR scope for cumulative dynamic archive, local historical output compatibility revalidation, and manual supporting-source lane.
  Quality checks python -m pytest tests/test_run_full_analysis.py tests/test_validate_generated_outputs.py tests/test_archive_run_outputs.py tests/test_research_support_scripts.py tests/test_publication_cumulative_database.py tests/test_cumulative_evidence_schemas.py -q; python -X utf8 run_full_analysis.py; python -X utf8 scripts/ingest_manual_supporting_sources.py --help; python -X utf8 scripts/revalidate_historical_outputs.py --help; python -X utf8 scripts/validate_manual_sources_gatekeeper.py --help; python -X utf8 scripts/build_cross_run_evidence_index.py --help; python -X utf8 scripts/validate_generated_outputs.py; python -X utf8 scripts/validate_research_source_outputs.py; python -m flake8 .; python -m mypy src scripts run_full_analysis.py main.py; python -m pytest tests/ -q.

- 2026-07-07 (versioned research data package generator and release manifest schema)
  Change type add update
  Scope scripts docs schemas tests governance
  Files affected scripts/build_versioned_research_data_package.py schemas/research_data_package_manifest.schema.json docs/CUMULATIVE_DATABASE_METHODOLOGY.md docs/DATA_RELEASE_POLICY.md tests/test_build_versioned_research_data_package.py tests/test_publication_cumulative_database.py CHANGELOG.txt MANIFEST_SOURCES.csv
  Summary Added a reproducible package generator for cumulative evidence release preparation (`scripts/build_versioned_research_data_package.py`) that builds normalized/flattened CSV + JSONL exports, optional XLSX/SAV outputs, schema validation gates, `RELEASE_MANIFEST.json`, `CHECKSUMS.sha256`, and APA-style citation template metadata. Added release-manifest schema contract and regression tests covering CLI argv forwarding and package artifact creation from deterministic fixtures.
  Reason PR-186 requires a code-level mechanism for producing a publication-ready, versioned research data package without committing the full empirical corpus in the Draft Code PR.
  Impact Enables auditable generation of downloadable dataset packages linked to commit/release metadata while keeping large empirical artifacts out of this code-only review scope.
  Provenance User requirement for publication-grade reproducibility/auditability and Git-referenced manifest/checksum release infrastructure.
  Quality checks python -X utf8 scripts/build_versioned_research_data_package.py --help; python -m pytest tests/test_build_versioned_research_data_package.py -q; python -m flake8 .; python -m mypy src scripts run_full_analysis.py main.py; python -m pytest tests/ -q; python scripts/generate_manifest.py.

- 2026-07-07 (smoke report generation for research-source validation parity)
  Change type fix add
  Scope scripts tests outputs governance
  Files affected scripts/smoke_scientific_bridge.py tests/test_research_support_scripts.py outputs/research_api_smoke_report.json CHANGELOG.txt
  Summary Updated smoke_scientific_bridge to use standard CLI entrypoint parsing, ASCII-safe status labels, and deterministic report emission to outputs/research_api_smoke_report.json (`--output` override supported). Added regression tests confirming report generation on both pass/fail paths and executed live-enriched smoke + full analysis + UTF-8 validators.
  Reason validate_research_source_outputs warned because the smoke-report artifact was not produced by the smoke script itself.
  Impact Research-source validation now finds the smoke report in normal local execution flows; warning count drops unless provider health itself is degraded.
  Provenance Local readiness audit for PR-186 finalization with live-enriched run path.
  Quality checks python -m pytest tests/test_research_support_scripts.py -q; python -X utf8 scripts/smoke_scientific_bridge.py --live-if-secrets-present; python -X utf8 run_full_analysis.py --analysis-input-mode live-enriched; python -X utf8 scripts/validate_generated_outputs.py; python -X utf8 scripts/validate_research_source_outputs.py.

- 2026-07-03 (cold-caching raw API payloads, Jaccard stemming, configurable priority weights)
  Change type add update
  Scope scripts analysis gap-model tests governance
  Files affected src/scientific_sources/models.py src/scientific_sources/crossref.py src/scientific_sources/elsevier_scopus.py src/scientific_sources/web_of_science.py src/scientific_sources/scival.py scripts/export_live_research_records.py scripts/archive_run_outputs.py schemas/run_archive_manifest.schema.json src/gap_model.py tests/test_scientific_sources.py tests/test_export_live_research_records.py tests/test_gap_model.py CHANGELOG.txt
  Summary (1) Added cold-caching of raw API JSON payloads: ProviderResult gains a raw_payload field; providers (Crossref, Scopus, WoS, SciVal) attach the verbatim API response before parsing; export_live_research_records.py writes each payload to outputs/research_sources/raw_api_payloads/<provider>_<sha8>.json in a self-describing envelope containing the payload SHA-256; archive_run_outputs.py counts cached files and writes raw_api_payloads_count to the run manifest. (2) Optimised Jaccard lexical coverage by adding a lightweight stdlib-only suffix-stripping stemmer (_stem_token) applied inside _name_tokens so morphological variants such as "digitalisation"/"digitalization" and "fisheries" collapse to the same stem before Jaccard comparison. (3) Made the multi-factor priority score configurable: compute_priority_score now accepts an optional weights dict; defaults to the existing equal 1/7 weighting, preserving backward compatibility.
  Reason Live API responses are mutable — provider re-indexing, privacy-policy changes, or temporary downtime cause data drift between runs; archiving the verbatim JSON payload before any normalisation allows physical replication without re-querying external networks. Jaccard false-negatives for morphologically related terms (e.g. "governance" vs "governed") reduce coverage accuracy; stdlib stemming removes the dependency on NLTK/spaCy while eliminating the most common suffix mismatches. Hard-coded equal weights prevent domain experts from elevating QMBD_axis_undercoverage or provider_confidence without modifying source code.
  Impact QMBD coverage analysis may reclassify previously "uncovered" demand items as "covered" (less pessimistic gap estimates) due to stemming. Priority scores are unchanged under default equal weights. Raw payload archive files are automatically included in _checksums.sha256 and manifest.json["files"] by the existing recursive archiver. New optional manifest field raw_api_payloads_count is backward-compatible with prior archived runs (field absent = 0 payloads).
  Provenance Plan grounded in repository architecture analysis; no new external domain evidence introduced.
  Quality checks python -m pytest tests/ -q (779 baseline + new tests); python -m flake8 src/ tests/ scripts/; python -m mypy src/.
  Reviewer GitHub Copilot Coding Agent

  2026-07-14
  Change type fix update
  Scope novelty-gates provider-canonicalization diagnostics packaging reporting tests workflow
  Files affected src/scientific_sources/cumulative_scientific_database.py scripts/compute_live_novelty_metrics.py src/scientific_sources/derived_competence_analysis.py scripts/export_live_research_records.py scripts/build_statistical_research_report.py scripts/build_live_cumulative_release_package.py .github/workflows/full-live-analysis.yml tests/test_cumulative_scientific_database.py tests/test_compute_live_novelty_metrics.py tests/test_derived_competence_analysis.py tests/test_export_live_research_records.py tests/test_full_live_analysis_workflow.py tests/test_live_cumulative_release_package.py CHANGELOG.txt
  Summary PR-191 follow-up: canonicalized provider identity for cumulative novelty/provider concentration metrics (Crossref/crossref/WoS aliases collapse deterministically), added producer-side run_novelty_metrics query_families_seen serialization from current-run provenance, tightened Gate E to evaluate single-family concentration directly from query_families_seen with explicit query_family_distribution_unavailable warning when provenance is missing, propagated review_required signals conservatively into derived demand status/validation counts, added Scopus per-query returned/normalized/contributed diagnostics outputs, and enforced current-run-only report/package assembly guards to block stale manifests.
  Reason Address remaining scientific-validity blockers from Request Changes without weakening Gates A-E or reinterpretation of negative-control runs.
  Impact Gate A/E now operate on canonical provider slugs end-to-end, query-family concentration no longer relies on query_diversity_score fallback, metadata-limited review_required signals cannot auto-promote into validated demand counts, Scopus diagnostics are exported per query for auditability, and report/package steps fail fast when current_run_id mismatches indicate stale assembly inputs.
  Quality checks python -m pytest tests/test_cumulative_scientific_database.py tests/test_compute_live_novelty_metrics.py tests/test_derived_competence_analysis.py tests/test_export_live_research_records.py tests/test_live_cumulative_release_package.py tests/test_full_live_analysis_workflow.py -v.
  Reviewer GitHub Copilot Coding Agent

- 2026-07-02 (PR-3B: evidence-first dynamic EQF micro-credential generation and validation)
  Change type add update fix
  Scope analysis pipeline scripts tests outputs governance
  Files affected run_full_analysis.py scripts/validate_generated_outputs.py tests/test_dynamic_credentials_generation.py tests/test_run_full_analysis.py scripts/build_derived.py MANIFEST_SOURCES.csv .gitignore outputs/credentials_database.json outputs/credentials_dynamic_database.json outputs/credentials_generation_rationale.json outputs/sector_qmbd_learning_pathways.json outputs/credentials_matrix.html outputs/report_index.html outputs/sector_pathways.json outputs/gaps_detailed.json outputs/gaps_by_sector_axis.csv outputs/gap_priority_ranking.csv CHANGELOG.txt
  Summary Implemented PR-3B evidence-first dynamic EQF micro-credential generation from PR-3A gap-model missing clusters, exported three new dynamic PR-3B artifacts, preserved backward-compatible outputs/credentials_database.json, normalized learning outcomes to avoid raw or truncated evidence-title fragments, and extended semantic validation for generated and review_required coverage.
  Reason PR-3B must convert gap-model evidence into defensible micro-credential outputs without forcing fixed 48 credentials where evidence is insufficient; missing EQF-sector combinations must be represented through review_required.
  Impact Downstream credential and report outputs now use dynamic evidence-first credentials. Audit traceability improves through outputs/credentials_generation_rationale.json and outputs/sector_qmbd_learning_pathways.json while legacy report and test consumers remain supported through outputs/credentials_database.json.
  Provenance PR-3A gap-model outputs are consumed, not refactored; generated_supply_evidence remains audit-only context, not verified supply.
  Quality checks python -m pytest tests/test_dynamic_credentials_generation.py -q; python -m pytest tests/ -q; python -m flake8 .; python -m mypy src scripts run_full_analysis.py main.py; python run_full_analysis.py --analysis-input-mode static; python scripts/generate_manifest.py; python scripts/validate_generated_outputs.py; Full Analysis workflow success; CodeQL 0 alerts.

- 2026-06-30 (PR A rev2: gap model — supply taxonomy, axis-sensitive coverage, gap-aware scoring)
  Change type update fix add
  Scope analysis pipeline scripts tests
  Files affected src/gap_model.py run_full_analysis.py tests/test_gap_model.py CHANGELOG.txt
  Summary Revised the four-layer gap model to address review feedback: (1) supply origin taxonomy now distinguishes static_baseline / existing_microcredential / generated_credential_previous_run / supply_file_unparsed; (2) Blue Clusters CSV is fully parsed into existing_microcredential supply evidence; (3) coverage rule is strictly axis-scoped (no cross-axis leakage) with Jaccard similarity threshold 0.30 replacing any-token overlap; (4) priority_score is gap-aware — gap_ratio and missing_count_normalized replace the constant sector_relevance factor (7-factor equal-weight formula); (5) provider extracted from lit_live_ ID slug, then explicit provider:/source_provider:/data_provider: keyword, then 'unknown' — thematic keywords and authorship are never used as provider; (6) gap_priority_ranking.csv extended with 8 audit columns; (7) 20 new tests covering all changed behaviours.
  Reason PR review required changes before merge: circular supply (credentials_database.json treated as verified supply), unparsed CSV claim, axis-leakage in coverage, non-gap-aware priority, inaccurate sector_relevance placeholder, thin priority CSV output, insufficient test coverage.
  Impact outputs/gap_priority_ranking.csv now includes Top_Origins, Top_Providers, Top_DOIs, Top_Titles, Year_Range, Average_Confidence, Coverage_Method, Supporting_Providers. Supply side gains existing_microcredential items from Blue Clusters CSV. Coverage false-positive rate reduced.
  Provenance src/gap_model.py — coverage rule, priority score, GapCluster.coverage_method; run_full_analysis.py — _collect_supply_from_microcredentials_csv(), _extract_provider_from_comp(), export_gap_priority_ranking_csv(); tests/test_gap_model.py — 20 new tests.
  Quality checks python -m pytest tests/test_gap_model.py -v (50 passed); python -m pytest tests/ -q (745 passed).

- 2026-06-30 (PR A: dynamic gap model)
  Change type add
  Scope analysis pipeline scripts tests
  Files affected src/gap_model.py run_full_analysis.py tests/test_gap_model.py CHANGELOG.txt
  Summary Added explicit four-layer gap model (demand_evidence / supply_evidence / missing_clusters / priority_score) in src/gap_model.py. Wired run_gap_model() orchestration function and three new export functions into run_full_analysis.py main() pipeline. Added 30 unit and integration tests.
  Reason The previous run_gap_analysis() treated supply as purely static baseline competences and lacked rich provenance on evidence items. The new model separates demand (literature + live API records) from supply (baseline + existing supply files), computes a multi-factor priority score, and produces richer outputs for downstream credential generation.
  Impact New output files: outputs/gaps_detailed.json, outputs/gaps_by_sector_axis.csv, outputs/gap_priority_ranking.csv. Existing outputs/gaps_summary.csv is preserved unchanged for backward compatibility.
  Provenance src/gap_model.py — new module; run_full_analysis.py sections 3b and 3c — new code; tests/test_gap_model.py — 30 new tests.
  Quality checks python -m pytest tests/test_gap_model.py -v (30 passed); python -m pytest tests/ -q (725 passed).

- 2026-06-29 (flake8 installation hardening in legacy CI workflow)
  Change type fix
  Scope CI tests governance
  Files affected .github/workflows/blank.yml tests/test_ci_workflow.py CHANGELOG.txt
  Summary Corrected PR-179 framing for run `27282229158`: flake8 was installed successfully in that run, and the failure was caused by repository-wide flake8 violations. Retained the narrower reliability improvement by executing lint via `python -m flake8` from the active Python environment and added a regression assertion for that invocation contract.
  Reason Address review feedback so PR-179 does not claim a missing-flake8 root cause for run `27282229158`, while preserving environment-bound linter invocation hardening.
  Impact Workflow execution is more robust to PATH ambiguity (`python -m flake8`), while repository-wide flake8/mypy failures remain unchanged pre-existing baseline debt outside this PR scope.
  Provenance PR-179 review correction: run `27282229158` installed flake8 successfully; lint violations caused the failure.
  Quality checks python -m pytest tests/test_ci_workflow.py -q; python -m pytest tests/ -q; flake8 src tests scripts (pre-existing unrelated failures); mypy src (pre-existing unrelated failures).

- 2026-06-29 (cross-run cumulative evidence index tables from run archive)
  Change type add
  Scope scripts tests governance
  Files affected scripts/build_cross_run_evidence_index.py tests/test_build_cross_run_evidence_index.py MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Added `scripts/build_cross_run_evidence_index.py` to generate cross-run longitudinal evidence tables directly from archived run artifacts (`manifest.json`, `research_sources/live_records*.json`, `analysis_outputs/cumulative_qmbd_records.json`) and `outputs/run_archive/cumulative_runs_index.csv`. The script outputs run summary, per-occurrence evidence rows, deduplicated cross-run evidence index, and a build report JSON for statistical workflows.
  Reason PR-2 requires a stable cross-run empirical evidence layer built from the methodological run archive before any PR-3 gap-model refactoring.
  Impact Introduces reproducible archive-derived cumulative evidence tables without changing existing gap analysis logic or EQF/micro-credential generation paths.
  Provenance PR-2 requirement: build cross-run cumulative evidence index from archived runs under `outputs/run_archive/runs/*`.
  Quality checks python -m pytest tests/test_build_cross_run_evidence_index.py -q; python -m pytest tests/ -q; flake8 src tests scripts (pre-existing unrelated failures); mypy src (pre-existing unrelated failures).

- 2026-06-29 (run archive manifest filename alignment)
  Change type fix
  Scope scripts tests governance
  Files affected scripts/archive_run_outputs.py scripts/validate_run_archive_integrity.py tests/test_archive_run_outputs.py tests/test_validate_run_archive_integrity.py CHANGELOG.txt
  Summary Updated run-archive manifest naming from `_run_manifest.json` to canonical `run_manifest.json` to match the agreed archive schema. Kept validator backward compatibility by accepting legacy `_run_manifest.json` in existing archives.
  Reason PR review flagged manifest filename mismatch against acceptance criteria for `outputs/run_archive/`.
  Impact New archives follow canonical filename while historical archives remain verifiable without migration.
  Provenance PR-1 review verdict: manifest filename does not meet agreed schema.
  Quality checks python -m pytest tests/test_archive_run_outputs.py tests/test_validate_run_archive_integrity.py tests/test_full_live_analysis_workflow.py tests/test_ci_workflow.py -q; python -m pytest tests/ -q; flake8 src tests scripts (pre-existing unrelated failures); mypy src (pre-existing unrelated failures).

- 2026-06-29 (run archive integrity enforcement in workflow and CI)
  Change type add
  Scope scripts CI tests governance
  Files affected scripts/validate_run_archive_integrity.py .github/workflows/full-live-analysis.yml .github/workflows/ci.yml tests/test_validate_run_archive_integrity.py tests/test_full_live_analysis_workflow.py tests/test_ci_workflow.py MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Added a dedicated validator for `outputs/run_archive/` that verifies manifest schema conformance, checksum correctness, file metadata consistency, and run index coverage for every archived run. Integrated this validator into full-live-analysis (strict presence required) and CI quick-mode gate (validate when archive exists) so archive corruption or drift is blocked by automation.
  Reason PR-2 requires explicit validation and enforcement of run archive integrity instead of relying only on archive creation.
  Impact CI now fails fast on corrupted/mutated archived artifacts; full-live-analysis confirms archive integrity immediately after writing archival outputs.
  Provenance PR-2 requirement: walidacja i egzekwowanie integralności outputs/run_archive/ w workflow/CI.
  Quality checks python -m pytest -q (baseline pass before changes); python -m pytest tests/test_validate_run_archive_integrity.py tests/test_full_live_analysis_workflow.py tests/test_ci_workflow.py -q; python -m pytest tests/ -q; python scripts/generate_manifest.py; flake8 src tests scripts (pre-existing unrelated failures); mypy src (pre-existing unrelated failures).

- 2026-06-14 (durable per-run archive for full live analysis)
  Change type add
  Scope scripts CI governance tests
  Files affected scripts/archive_run_outputs.py .github/workflows/full-live-analysis.yml tests/test_archive_run_outputs.py tests/test_full_live_analysis_workflow.py CHANGELOG.txt
  Summary Added a dedicated archival script that captures the complete full-live-analysis output set into outputs/run_archive/runs/<run_id>, including checksums, run manifest metadata, and a cumulative JSONL run index. Integrated archival into the full-live-analysis workflow after output validation and persisted validation_state.json for reproducibility context.
  Reason The repository previously preserved only latest snapshots in outputs/ and short-lived workflow artifacts; methodological requirements need durable, repo-side per-run corpus storage.
  Impact Existing canonical outputs/ filenames remain unchanged for backward compatibility, while each workflow run now gains an immutable archive copy suitable for longitudinal evidence tracking.
  Provenance Audit findings on snapshot-only outputs and missing cross-run corpus persistence.
  Quality checks python -m pytest -q (baseline pass); python -m pytest tests/test_archive_run_outputs.py tests/test_full_live_analysis_workflow.py -q; flake8 src tests (pre-existing unrelated failures); mypy src (pre-existing unrelated failures).

- 2026-06-08 (export workflow preflight provider scope)
  Change type fix
  Scope CI tests governance
  Files affected .github/workflows/export-research-records.yml tests/test_export_research_records_workflow.py CHANGELOG.txt
  Summary Scoped the export workflow preflight credential validation to the requested provider list and kept the default providers CI-safe (crossref,scopus). Relaxed regression assertions to verify semantic flags/arguments without brittle line-continuation formatting coupling.
  Reason The preflight step previously validated optional providers outside the selected export scope, causing avoidable failures. Governance CI also requires changelog traceability for substantive workflow/test updates.
  Impact Manual and CI-like runs no longer fail due to unrelated provider credentials; regression tests still lock in provider scoping/default behavior while tolerating harmless YAML formatting changes.
  Provenance PR #178 review feedback and governance-and-repro CI changelog guard.
  Quality checks python -m pytest tests/test_export_research_records_workflow.py -q; python scripts/changelog_guard.py --base-ref main.

- 2026-06-04 (restrict default providers in full-live-analysis workflow)
  Change type fix
  Scope CI governance
  Files affected .github/workflows/full-live-analysis.yml CHANGELOG.txt
  Summary Removed scival and microsoft_graph from the default providers list in full-live-analysis.yml (both the workflow_dispatch input default and the env REQUESTED_PROVIDERS fallback). Default is now crossref,scopus,wos — providers with guaranteed CI secret coverage.
  Reason scival and microsoft_graph do not have guaranteed secrets in CI; including them as defaults caused unnecessary health-preflight noise and potential credential failures. This mirrors the earlier removal of google_drive from CI defaults.
  Impact Scheduled and unparameterised workflow_dispatch runs now only invoke crossref, scopus, and wos by default. Users can still pass scival,microsoft_graph explicitly when the secrets are configured.
  Provenance Recommendation from governance review (Opcja 2 - Ograniczenie domyślnych dostawców).
  Quality checks python3 scripts/changelog_guard.py --changed-file .github/workflows/full-live-analysis.yml --changed-file CHANGELOG.txt (passed).

- 2026-06-04 (manifest update for cumulative merge script)
  Change type add
  Scope scripts governance
  Files affected scripts/cumulative_update_merge_full_live_enriched_and_static_all_outputs_fragmented_analysis.py MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Added cumulative output merge utility script and regenerated MANIFEST_SOURCES.csv to include the new file so the governance-and-repro CI manifest reproducibility check passes.
  Reason The new script was committed without updating the manifest, causing the CI governance job to fail with a manifest drift error.
  Impact MANIFEST_SOURCES.csv now matches the repository contents; CI governance checks pass.
  Provenance Commit 241992e (chore: add cumulative output merge utility).
  Quality checks python3 scripts/generate_manifest.py (935 entries, no drift); python3 scripts/changelog_guard.py (passed).

- 2026-06-03 (offline-mode live workflow gating)
  Change type fix
  Scope CI scripts tests governance
  Files affected .github/workflows/full-live-analysis.yml scripts/validate_research_source_outputs.py tests/test_research_support_scripts.py CHANGELOG.txt
  Summary Gated provider health preflight behind OFFLINE_MODE so offline runs avoid network probes, and removed mandatory health-file requirement from the offline validation path. Updated research-source health validation to warn (not fail) on invalid/rate-limited provider statuses so strict credential gating remains controlled by check_research_api_health.py --require-valid.
  Reason Review feedback highlighted that offline executions still depended on networked health checks and that health-status invalids could fail downstream validation even when strict credential gating was disabled.
  Impact Offline workflow dispatch now behaves consistently with its no-network intent; live validation still enforces health/live artifacts for online runs; invalid provider statuses remain visible as warnings.
  Provenance PR #175 review threads for full-live-analysis.yml and validate_research_source_outputs.py.
  Quality checks python -m pytest tests/test_research_support_scripts.py tests/test_validate_generated_outputs.py -q (passed).

- 2026-05-22 (deep consistency validation)
  Change type fix
  Scope scripts pipeline tests
  Files affected run_full_analysis.py scripts/export_live_research_records.py src/core.py src/axis_classifier.py tests/test_research_queries_config.py
  Summary Reformatted touched files with black, fixed a trailing-blank-line flake8 warning, and improved type-check clarity in run_full_analysis.py (TypeAlias for TMBDAxis, safer sentence-classification normalization, explicit cast for slugify). Marked PyYAML import as untyped for mypy without changing runtime behaviour.
  Reason Ensure the new live-enriched pipeline remains internally consistent and tool-clean when running optional developer checks (black/flake8/mypy) without expanding scope into unrelated pre-existing type errors.
  Impact No behavioural changes intended; improves maintainability and audit stability for the live-enriched analysis code paths.
  Provenance Internal consistency review per PROMPT 1 master plan and governance checks in scripts/sync_audit.py --strict.
  Quality checks python -m black (reformatted); python -m flake8 (touched files clean); pytest tests/ (passed); python scripts/sync_audit.py --strict (passes after commit).

- 2026-05-22 (CI-safe fixes for hardening patch)
  Change type fix
  Scope scripts governance
  Files affected scripts/validate_research_source_outputs.py scripts/sync_audit.py
  Summary Removed generated_at from required-key check in validate_research_source_outputs.py (field was intentionally dropped from the neutral baseline). Updated sync_audit _capabilities_semantically_equal to also ignore the note field, preventing false drift warnings when environment-dependent note text differs.
  Reason The neutral capabilities baseline no longer includes generated_at; requiring it caused validate_research_source_outputs.py to fail. The note field varies between baseline ("repository baseline state") and runtime ("runtime environment"), producing false semantic drift.
  Impact CI quick-mode-gate and local sync_audit --strict now pass cleanly without false positives from non-semantic fields.
  Provenance PR #173 reviewer feedback (commit 7cf6f3d); AGENT_WORKING_AGREEMENT § 3 deterministic output policy.
  Quality checks python scripts/validate_research_source_outputs.py (passed); python scripts/validate_generated_outputs.py (passed); git diff --exit-code -- outputs/ MANIFEST_SOURCES.csv (no drift).

- 2026-05-22 (live-enriched append-only QMBD workflow)
  Change type add update
  Scope pipeline scripts CI tests data
  Files affected .github/workflows/full-live-analysis.yml config/research_queries.yml scripts/export_live_research_records.py run_full_analysis.py src/axis_classifier.py src/core.py src/scientific_sources/models.py schemas/research_metadata.schema.json tests/test_export_live_research_records.py tests/test_research_queries_config.py outputs/ MANIFEST_SOURCES.csv
  Summary Added an opt-in scheduled/manual GitHub Actions workflow for live-enriched analysis. Updated live export to retain sector provenance and optional language metadata, strengthened DOI→title→source_id dedup keys, expanded HYDRONIZATION keyword coverage, and updated HTML outputs to show QMBD axis columns and live/static breakdown when available.
  Reason Reduce static-count and sector-query mismatch bias by aligning live query groups to the 12 canonical sectors and carrying sector evidence through triangulation into the append-only analysis layer.
  Impact Static outputs remain valid; live-enriched runs can merge and report additional evidence without assuming fixed competence counts. Stage-2 schema now allows language metadata while still forbidding restricted abstract/citation fields.
  Provenance PROMPT 1 Master Implementation Prompt (sector-query mismatch, DOI bias, Hydronization underpopulation); existing QMBD pipeline in run_full_analysis.py and scripts/export_live_research_records.py.
  Quality checks python -m pytest tests/ (passed); python run_full_analysis.py --analysis-input-mode static (generated outputs); python scripts/validate_generated_outputs.py (passed); python scripts/generate_manifest.py (deterministic).

- 2026-05-21 (hardening patch)
  Change type add
  Scope governance scripts CI tests
  Files affected .github/PULL_REQUEST_TEMPLATE.md scripts/sync_audit.py .github/workflows/ci.yml tests/test_sync_audit.py CHANGELOG.txt
  Summary Added PR template enforcing AGENT_WORKING_AGREEMENT contribution fields, a sync-audit script for multi-device drift detection, and a CI quick-mode-gate job that runs deterministic static analysis + output sync verification on every PR.
  Reason Observed destabilization of syncing between local, remote, Android, API providers, Google Drive, and SharePoint. The hardening patch closes the enforcement gap between the documented working agreement and actual CI/PR policy, reducing risk of silent drift, merge conflicts, and failed tests.
  Impact PRs now require explicit objective/source-basis/axis-logic/artifact declarations. CI catches output drift and manifest desynchronization before merge. Contributors can run `python scripts/sync_audit.py --strict` locally to pre-validate sync state.
  Provenance docs/AGENT_WORKING_AGREEMENT.md § 3; COPILOT_MCP_SETUP.md (sync architecture); .github/workflows/ci.yml governance-and-repro pattern.
  Quality checks python -m pytest tests/test_sync_audit.py -q (all passed); python scripts/sync_audit.py (passed); yamllint .github/workflows/ci.yml (valid).

- 2026-05-21
  Change type add update fix
  Scope scripts governance docs tests
  Files affected scripts/run_research_api_full.sh scripts/run_research_api_full.ps1 scripts/validate_generated_outputs.py tests/test_validate_generated_outputs.py docs/AGENT_WORKING_AGREEMENT.md docs/ONE_CLICK_RESEARCH_RUNBOOK.md README.md CHANGELOG.txt
  Summary Implemented a shared Agent Working Agreement checklist and standardized one-command workflow modes (`quick`, `full-static`, `full-live`) with explicit provider capability diagnostics. Strengthened output semantic validation with cumulative QMBD schema checks and stricter micro-credential completeness requirements.
  Reason The collaboration plan required a single operational contract for all contributors plus stronger automation and consistency gates aligned to repository scientific aims and governance traceability.
  Impact Improves cross-contributor alignment, makes standard execution modes explicit, and hardens validation for cumulative QMBD integrity and required credential fields before downstream analysis/review.
  Provenance docs/AGENT_WORKING_AGREEMENT.md (new contract); scripts/run_research_api_full* mode orchestration; scripts/validate_generated_outputs.py new gates; tests/test_validate_generated_outputs.py regression coverage.
  Quality checks python -m pytest tests/ -q (621 passed); python -m pytest tests/test_validate_generated_outputs.py tests/test_run_full_analysis.py tests/test_script_cli.py tests/test_main_entrypoints.py -q (89 passed); python scripts/validate_generated_outputs.py passed; ./scripts/run_research_api_full.sh --mode quick passed; flake8 scripts/validate_generated_outputs.py tests/test_validate_generated_outputs.py passed; mypy scripts/validate_generated_outputs.py passed.
  Reviewer GitHub Copilot Coding Agent

- 2026-05-19
  Change type add fix update
  Scope tests outputs governance
  Files affected tests/test_convert_pdfs_to_txt.py tests/test_cumulative_fragment_analysis.py tests/test_scientific_sources.py tests/test_smoke_scientific_bridge.py outputs/cumulative_qmbd_records.json MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Expanded coverage for PDF conversion, cumulative fragment analysis, scientific bridge smoke paths, and scientific-source provider branches; regenerated cumulative QMBD output and manifest artifacts to keep committed outputs synchronized with the current pipeline state.
  Reason PR #170 changes substantive test, output, and manifest artifacts, so the governance rule requires explicit changelog traceability.
  Impact Improves regression coverage for scientific-source and analysis utilities while preserving reproducible output state for full-analysis and governance checks.
  Provenance PR #170; coverage-expansion test changes; regenerated outputs/cumulative_qmbd_records.json; regenerated MANIFEST_SOURCES.csv; governance-and-repro branch changelog sync commit.
  Quality checks CI test-suite passed on Python 3.10, 3.11, and 3.12; manifest reproducibility check passed for MANIFEST_SOURCES.csv; governance-and-repro expected to pass after this CHANGELOG entry.
  Reviewer GitHub Copilot Coding Agent

- 2026-05-19
  Change type add fix update
  Scope scripts tests governance
  Files affected scripts/changelog_guard.py .github/workflows/ci.yml tests/test_changelog_guard.py MANIFEST_SOURCES.csv CHANGELOG.txt
  Summary Extracted the CI CHANGELOG enforcement rule into a reusable Python guard and added direct regression coverage for pull-request changed-file detection, normalization, and CLI outcomes.
  Reason Coverage analysis showed the CHANGELOG policy lived only in an inline workflow shell block with no dedicated tests, leaving pull/commit-style enforcement behavior unverified.
  Impact Makes the governance rule testable and easier to maintain while preserving the existing lightweight policy for substantive tracked artifacts.
  Provenance `.github/workflows/ci.yml` CHANGELOG enforcement step; `scripts/changelog_guard.py`; `tests/test_changelog_guard.py`.
  Quality checks `python scripts/generate_manifest.py` passed; `python -m pytest tests/test_changelog_guard.py -q` passed (26 tests); `python -m pytest tests/ -q --cov=src --cov=scripts --cov-repo[...] 
  Reviewer GitHub Copilot Coding Agent

- 2026-05-19
  Change type fix update
  Scope src tests
  Files affected src/axis_classifier.py tests/test_axis_classifier.py tests/test_check_research_api_health.py tests/test_run_full_analysis.py CHANGELOG.txt
  Summary Refined axis classification behavior and expanded regression coverage for research API health checks and full-analysis orchestration.
  Reason CI requires changelog updates when substantive tracked artifacts change, and this PR modifies source and test files.
  Impact Preserves governance traceability for classifier and orchestration changes; no substantive source/test update is left undocumented.
  Provenance PR #162.
  Quality checks CI governance-and-repro rule satisfied after adding CHANGELOG.txt.
  Reviewer GitHub Copilot Chat Assistant

- 2026-05-19
  Change type fix update
  Scope src tests governance
  Files affected src/scientific_sources/crossref.py tests/test_scientific_sources.py CHANGELOG.txt
  Summary Stopped the Crossref search request from asking for abstract metadata and added regression coverage ensuring the provider requests only the export-safe metadata fields it actually uses.
  Reason The methodological-hardening follow-up found that Crossref still requested the abstract field even though the normalized provider now discards it. Removing the request tightens compliance[...]
  Impact Reduces unnecessary retrieval of restricted metadata while preserving Crossref search behavior and keeping provider outputs aligned with the intended compliance-safe contract.
  Provenance PR #162.
  Quality checks python3 -m pytest tests/test_scientific_sources.py -q passed (85 tests); python3 -m pytest tests/test_axis_classifier.py tests/test_run_full_analysis.py tests/test_export_live_res[...] 
  Reviewer GitHub Copilot Coding Agent

- 2026-05-19
  Change type fix update
  Scope src tests outputs governance
  Files affected run_full_analysis.py tests/test_run_full_analysis.py outputs/competences_full_database.json outputs/credentials_database.json outputs/cumulative_qmbd_records.json outputs/gaps_by_[...]
  Summary Hardened sentence-level QMBD resolution so fallback OCEANIC classifications without keyword evidence no longer dominate primary-axis selection. Live sentence resolution now ignores unsup[...]
  Reason The sentence-level audit path was methodologically over-classifying generic/no-signal material as OCEANIC, which biased literature and live-record primary-axis results. This change keeps [...]
  Impact Regenerated outputs now show a materially broader literature-axis distribution and more defensible primary-axis assignment across cumulative QMBD artifacts.
  Provenance PR #162.
  Quality checks python3 -m pytest tests/test_axis_classifier.py tests/test_run_full_analysis.py tests/test_export_live_research_records.py tests/test_scientific_sources.py -q passed (227 tests); [...]
  Reviewer GitHub Copilot Coding Agent

- 2026-05-19
  Change type fix update
  Scope src outputs governance
  Files affected run_full_analysis.py scripts/export_live_research_records.py src/axis_classifier.py src/scientific_sources/models.py src/scientific_sources/crossref.py outputs/competences_full_da[...]
  Summary Regenerated committed full-analysis outputs after the QMBD pipeline hardening changes and documented the associated compliance-safe serialization, sanitized live audit payloads, and shar[...]
  Reason The PR changed substantive generator behavior, so outputs/ needed to be brought back in sync with `python run_full_analysis.py`, and governance CI requires a changelog entry when tracked [...]
  Impact Restores deterministic PR state for full-analysis sync checks and preserves traceability for the hardened live-audit and scientific-source handling changes.
  Provenance PR #162.
  Quality checks python3 run_full_analysis.py completed twice locally; regenerated outputs reviewed; manifest regeneration remained deterministic.
  Reviewer GitHub Copilot Coding Agent

- 2026-05-19
  Change type fix update
  Scope src tests
  Files affected src/scientific_sources/models.py tests/test_axis_classifier.py tests/test_export_live_research_records.py tests/test_run_full_analysis.py CHANGELOG.txt
  Summary Updated the scientific sources model and aligned test coverage for axis classification, live research export, and full-analysis orchestration.
  Reason CI requires changelog updates when substantive tracked artifacts change, and these code/test updates alter repository behaviour and verification scope.
  Impact Preserves governance traceability for source-model and test-suite changes; no undocumented substantive changes remain in the PR.
  Provenance PR #163.
  Quality checks CI governance-and-repro rule satisfied after adding CHANGELOG.txt.
  Reviewer GitHub Copilot Coding Agent

2026-05-14 (Policy-driven live triangulation, QMBD-4 enrichment, and Microsoft Graph integration)
Change type add update
Scope scripts src docs config
Files affected scripts/export_live_research_records.py scripts/build_tmbd_dictionary.py scripts/check_research_api_health.py scripts/check_research_env.py scripts/audit_research_api_config.py scri[...]
Summary Implemented policy-driven live triangulation with explicit provider-priority contract (Loop 1: identity/provenance deduplication; Loop 2: thematic/QMBD axis assignment). Added durable audi[...]
Reason The previous implicit registry-order deduplication gave no audit trail when two providers returned the same record. The new double-loop design ensures identity winners are policy-driven and[...]
Impact export_live_research_records.py now emits triangulated and raw artifacts. run_full_analysis.py defaults to live_records_triangulated.json for enrichment. Microsoft Graph provider is operati[...]
Provenance config/research_provider_policy.yml (new); src/scientific_sources/microsoft_graph.py; scripts/export_live_research_records.py.
Quality checks python -m pytest -q passed locally.
Reviewer GitHub Copilot Coding Agent

2026-05-14
Change type fix add
Scope scripts tests
Files affected scripts/export_live_research_records.py tests/test_export_live_research_records.py
Summary Hardened live research export CLI query-file validation. yaml.safe_load() is now wrapped in a try/except yaml.YAMLError block that emits a clear parse-error message including the file pat[...]
Reason Previously a syntactically invalid YAML query file would crash the script with an unhandled traceback. Provider inputs with differing case or surrounding whitespace were silently rejected.[...]
Impact scripts/export_live_research_records.py now fails fast with actionable operator messages for all YAML and provider-input edge cases.
Provenance scripts/export_live_research_records.py (CLI entrypoint); tests/test_export_live_research_records.py (integration tests).
Quality checks python -m pytest tests/test_export_live_research_records.py -q passed (45 tests).
Reviewer GitHub Copilot Coding Agent

2026-05-11
Change type fix add
Scope triangulator tests
Files affected src/cumulative_analysis/triangulator.py tests/test_cumulative_triangulator_dynamic_priority.py MANIFEST_SOURCES.csv
Summary Fixed a bug in CumulativeTriangulator where a later dynamic ingestion (e.g. Scopus) could overwrite a record already upgraded from the static baseline by an earlier dynamic provider (e.g.[...]
Reason When multiple dynamic providers returned records sharing the same DOI or normalised title, the second provider's data silently overwrote the first provider's upgrade, losing provenance acc[...]
Impact CumulativeTriangulator.triangulate() now consistently returns the first-ingested dynamic provider's record when duplicates exist, matching the documented deduplication contract. Downstream[...]
Provenance src/cumulative_analysis/triangulator.py (deduplication logic); src/scientific_sources/models.py (LiteratureRecord); tests/test_cumulative_triangulator.py (existing triangulator test su[...]
Quality checks python -m pytest -q passed; new tests test_first_dynamic_doi_match_preserved_after_static_upgrade and test_first_dynamic_title_match_preserved_after_static_upgrade both pass.
Reviewer GitHub Copilot Coding Agent

2026-05-11 (Stage 1 documentation reconciliation — QMBD wording and citation-count rule)
Change type fix
Scope governance
Files affected docs/overview.md docs/onboarding_new_provider.md docs/providers.md docs/RESEARCH_SOURCE_GOVERNANCE.md CHANGELOG.txt
Summary Reconciled the remaining Stage 1 documentation clashes without changing code. docs/overview.md now describes the implemented QMBD 4-axis model (including Hydronization) instead of the obs[...]
Reason Review feedback concluded that Stage 1 enforcement was already correct in code and tests, but the documentation foundation was not yet officially complete because the human contracts still[...]
Impact Documentation only. No code, test, CI, or schema behaviour changes. Stage 1 governance text now matches `_to_stage1_compliant_dict()` and the current 4-axis `BlueDynamicsAxis` enum.
Provenance src/core.py:BlueDynamicsAxis (QMBD with HYDRONIZATION); scripts/export_live_research_records.py:_to_stage1_compliant_dict() and STAGE1_CSV_FIELDS (citation-count drop rule); tests/test[...]
Quality checks Baseline `python -m pytest -q` passes locally before and after the documentation update. `flake8 src tests` and `mypy src` remain non-clean due to pre-existing unrelated issues.
Reviewer GitHub Copilot Coding Agent

2026-05-11 (governance docs provider-field clarification and manifest refresh)
Change type fix update
Scope governance
Files affected docs/RESEARCH_SOURCE_GOVERNANCE.md MANIFEST_SOURCES.csv CHANGELOG.txt
Summary Clarified Scopus and Web of Science provider-specific notes in docs/RESEARCH_SOURCE_GOVERNANCE.md to explicitly distinguish provider-returned bibliographic fields (title, authors, year, D[...]
Reason The prior wording could be read as implying that the provider-specific field list was the complete allowed storage schema, creating ambiguity for contributors. The manifest update is a mec[...]
Impact Documentation only for the governance doc fix. MANIFEST_SOURCES.csv: many .py file rows changed from type "other" / text_available=no to type "script" / text_available=yes — no runtime o[...]
Provenance docs/RESEARCH_SOURCE_GOVERNANCE.md Stage 1 permissible fields table; scripts/generate_manifest.py classification logic (.py → script); .github/workflows/ci.yml CHANGELOG enforcement [...]
Quality checks CI CHANGELOG enforcement rule satisfied. No code or test changes.
Reviewer GitHub Copilot Coding Agent

2026-05-10 (QMBD keyword governance fix — remove ungrounded blue subjectivity keyword)
Change type fix
Scope src tests governance
Files affected src/axis_classifier.py tests/test_axis_classifier.py CHANGELOG.txt
Summary Removed "blue subjectivity" from the HYDRONIZATION keyword set in AxisClassifier.KEYWORD_AXIS_MAP after failing to locate a source document citation in the repository. Per LLM_CONTEXT_INS[...]
Reason Review feedback identified the [CITATION_REQUIRED] placeholder as a compliance risk. Spelling of HYDRONIZATION is intentional and matches the established codebase usage in scripts/cumulati[...]
Impact src/axis_classifier.py: HYDRONIZATION keyword count reduced from 4 to 3. Texts containing only "blue subjectivity" now fall through to OCEANIC (governance-first default). No other behaviou[...]
Provenance docs/literature/Bartłomiejskie Cocco krytyka oceanocentryzmu.txt:912 (hydrosocial); Bartłomiejski Cocco Performatywność wody morza oceanu.txt:2064-2066 (wet ontology); scripts/cumu[...]
Quality checks pytest: all tests pass. No [CITATION_REQUIRED] placeholders remain in production code.
Reviewer GitHub Copilot Coding Agent


2026-05-10 (QMBD synthesis — Stage 1 compliance, triangulator, HYDRONIZATION axis)
Change type add update
Scope governance scripts src tests
Files affected scripts/export_live_research_records.py src/core.py src/axis_classifier.py src/cumulative_analysis/__init__.py src/cumulative_analysis/triangulator.py tests/test_export_live_resear[...]
Summary Three coordinated changes: (1) Stage 1 governance audit — scripts/export_live_research_records.py now routes JSON and CSV exports through _to_stage1_compliant_dict(), explicitly droppin[...]
Reason Reconcile PR #125 (Stage 1 Human Contracts), PR #119 (Live API Export), and the Manus methodological review to establish a unified QMBD framework. The governance audit ensures no restricte[...]
Impact scripts/export_live_research_records.py: JSON output no longer includes citation_count, abstract_available, or abstract_stored; CSV now includes licence_note column. src/core.py: BlueDynam[...]
Provenance docs/licensing_and_compliance.md (Stage 1 bibliographic-only rules); scripts/cumulative_fragment_analysis.py:qmbd_label_from_text (hydronization keyword basis); DATA_GOVERNANCE.txt (FA[...]
Quality checks pytest: all tests pass. Compliance filter verified by new TestStage1ComplianceFilter suite. Triangulator verified by new test_cumulative_triangulator.py. QMBD axis verified by new [...]
Reviewer GitHub Copilot Coding Agent

2026-05-10 (docs/providers.md — QMBD alignment and Stage 1 compliance section)
Change type update
Scope governance
Files affected docs/providers.md CHANGELOG.txt
Summary Updated docs/providers.md to reflect the QMBD (4-axis) model replacing TMBD references: renamed "TMBD relevance" column to "QMBD axis relevance" in the summary table, updated SciVal secti[...]
Reason The existing docs/providers.md was drafted against the 3-axis TMBD model. The QMBD synthesis PR extended the axis model to 4 dimensions and introduced a compliance filter and triangulation[...]
Impact Documentation only. No code, CI, or schema changes.
Provenance scripts/export_live_research_records.py:_to_stage1_compliant_dict() (compliance filter); src/cumulative_analysis/triangulator.py (triangulation engine); src/core.py:BlueDynam[...] 
Quality checks No tests for documentation; prose reviewed for consistency with implementation.
Reviewer GitHub Copilot Coding Agent

2026-05-08 (Stage 1 documentation — accuracy corrections)
Change type fix
Scope governance
Files affected docs/overview.md docs/onboarding_new_provider.md CHANGELOG.txt
Summary Corrected two inaccurate claims in the Stage 1 documentation layer. In docs/overview.md, removed the incorrect assertion that literature records automatically map to a TMBD axis (Literatu[...]
Reason Code review on PR identified a factual mismatch between the documentation and the actual LiteratureRecord data model and existing provider implementations (ElsevierScopusProvider, GoogleDr[...]
Impact Documentation only. No code, CI, or schema changes.
Provenance src/scientific_sources/models.py (LiteratureRecord has no axis field); src/scientific_sources/elsevier_scopus.py and google_drive.py (credential check pattern in __init__ + capability [...]
Quality checks No tests affected. Governance files only.
Reviewer GitHub Copilot Coding Agent

2026-05-08 (Cumulative fragment analysis workflow)
Change type add
Scope scripts tests governance
Files affected scripts/cumulative_fragment_analysis.py tests/test_cumulative_fragment_analysis.py docs/overview.md CHANGELOG.txt
Summary Added a cumulative, fragment-level semantic analysis workflow that deduplicates DOI-linked sentences across runs using a stable hash, then records both strict TMBD axis classification (vi[...]
Reason PR review requested implementing fragment-level provenance so semantic frequencies are not double-counted when the same source is processed multiple times, while keeping LiteratureRecord a[...]
Impact Adds a new derived-state workflow for researchers; no changes to existing provider ingestion or core data models.
Provenance src/axis_classifier.py (deterministic TMBD classifier); DATA_GOVERNANCE.txt (derived outputs are append-only and do not overwrite raw baselines).
Quality checks python -m pytest -q passes locally; flake8/mypy remain non-clean due to pre-existing issues.
Reviewer Codex CLI agent

2026-05-04 (Stage 1 documentation layer — human contracts)
Change type add
Scope governance
Files affected docs/overview.md docs/providers.md docs/onboarding_new_provider.md docs/licensing_and_compliance.md CHANGELOG.txt
Summary Added four plain-English contract documents forming the Stage 1 documentation layer of the repository upgrade plan. docs/overview.md describes what the repository is and is not, documents[...]
Reason CI enforcement already existed before this documentation was written. Stage 1 documents intent and rules in human-readable form before any additional enforcement is layered on top, followi[...]
Impact No code changes. No CI changes. These documents are non-executable. Future enforcement stages (Stage 4 and beyond) may reference these documents as the written basis for rules that become [...]
Provenance Derived from DATA_GOVERNANCE.txt, CITATION.txt, LLM_CONTEXT_INSTRUCTION.txt, docs/RESEARCH_SOURCE_GOVERNANCE.md, docs/RESEARCH_API_CICD_SETUP.md, and src/scientific_sources/ module ins[...]
Quality checks No tests affected. Governance files only. CI conflict-marker check and governance-file-presence check unaffected.
Reviewer GitHub Copilot Coding Agent

2026-05-03 (validate and normalise --providers CLI argument)
Change type fix
Scope scripts tests
Files affected scripts/export_live_research_records.py tests/test_export_live_research_records.py CHANGELOG.txt
Summary Normalised provider names to lowercase on parse (preventing silent mismatches like "Crossref" vs "crossref"), rejected empty provider lists, and added validation of provider names against[...]
Reason SourceCapability.name values are lowercase (crossref, scopus, etc.). Passing "Crossref" or a typo like "crossreff" previously silently resulted in zero providers being queried, producing e[...]
Impact Export script now rejects unrecognised provider names and normalises case; existing users passing lowercase names see no behaviour change.
Provenance Review thread pullrequestreview-4207014085, comment at scripts/export_live_research_records.py:285.
Quality checks python -m pytest -q passes (341 passed, 1 skipped); flake8 scripts/export_live_research_records.py clean.
Reviewer GitHub Copilot Coding Agent

2026-04-30 (fix provider ordering in coverage report and E305 blank-line lint)
Change type fix
Scope scripts tests
Files affected scripts/export_live_research_records.py tests/test_export_live_research_records.py CHANGELOG.txt
Summary Fixed provider_name attribution in coverage rows: instead of indexing into provider_list (which reflects user input order), derive ordered_provider_names from registry.list_capabilities()[...]
Reason SourceRegistry.search() preserves the registry's internal _providers order, not the caller's requested provider order. Indexing provider_list[i] therefore mislabelled coverage rows when th[...]
Impact Coverage CSV now correctly attributes provider names for all result positions, including zero-record providers.
Provenance Code review comment on scripts/export_live_research_records.py lines 338-343.
Quality checks python -m pytest tests/test_export_live_research_records.py -v passes (27 passed); flake8 scripts/export_live_research_records.py clean.
Reviewer GitHub Copilot Coding Agent

2026-04-30 (preflight API health check)
Change type add
Scope scripts workflows docs
Files affected scripts/check_research_api_health.py .github/workflows/export-research-records.yml .github/workflows/research-api-smoke.yml docs/RESEARCH_API_CICD_SETUP.md docs/ONE_CLICK_RESEARCH_[...]
Summary Added scripts/check_research_api_health.py, a lightweight preflight probe that verifies provider credentials before long-running live export workflows. Returns explicit machine-readable s[...]
Reason Live export workflows would previously fail mid-run with opaque API errors when credentials were revoked or rate-limited; a fast preflight gate saves CI time and gives actionable diagnosti[...]
Impact CI live-export workflows now fail fast with a clear error when credentials are invalid, rather than after full query execution. Sanitized health status artifact is retained per run.
Provenance PR #120 (codex/extend-preflight-logic-for-api-health-checks).
Quality checks python -m py_compile scripts/check_research_api_health.py passes; python -m pytest -q passes.
Reviewer GitHub Copilot Task Agent

2026-04-30 (guard against empty/invalid YAML query config)
Change type fix
Scope scripts tests
Files affected scripts/export_live_research_records.py tests/test_export_live_research_records.py CHANGELOG.txt
Summary Added an isinstance(query_config, dict) guard after yaml.safe_load in export_live_research_records.py (line 268 area) so that empty files, comment-only files, or non-dict scalars produce [...]
Reason yaml.safe_load returns None for empty/comment-only files and non-dict scalars for other malformed inputs; calling .get() on those values raised AttributeError and broke the export workflow[...]
Impact Export script now exits cleanly with an actionable error message when the query YAML file is present but empty or malformed. No change to behaviour for valid query files.
Provenance Code review comment on scripts/export_live_research_records.py line 268 (P2 badge).
Quality checks python -m pytest tests/test_export_live_research_records.py -v passes (25 passed).
Reviewer GitHub Copilot Coding Agent

2026-04-30 (fix stale MANIFEST_SOURCES.csv)
Change type fix
Scope governance
Files affected MANIFEST_SOURCES.csv CHANGELOG.txt
Summary Regenerated MANIFEST_SOURCES.csv to remove 907 stale src/morskamary/ path entries that were no longer produced by generate_manifest.py, resolving the governance-and-repro manifest reprodu[...]
Reason The first commit in this PR inadvertently committed an older MANIFEST_SOURCES.csv snapshot containing src/morskamary/ prefixed entries; the CI governance check runs generate_manifest.py fr[...]
Impact CI governance-and-repro manifest check passes. The test-suite Python 3.10 failure (pandas==3.0.2 unavailable) is a pre-existing issue on main and is not related to this PR.
Provenance python scripts/generate_manifest.py output (898 entries).
Quality checks git diff MANIFEST_SOURCES.csv shows 907 deletions and 0 additions after regeneration.
Reviewer GitHub Copilot Coding Agent

2026-04-29 (test coverage improvements to 100%)
Change type add
Scope tests
Files affected tests/test_axis_classifier.py tests/test_nlp_reliability.py tests/test_scientific_sources.py CHANGELOG.txt
Summary Added 14 new test cases to cover the 17 previously uncovered lines across src/, bringing test coverage from 98% to 100%. New tests cover: map_dimension_to_axis with empty/whitespace input[...]
Reason Coverage analysis identified 17 uncovered lines in 10 source files, all reachable through legitimate production code paths.
Impact All 686 executable statements in src/ are now exercised by the test suite (python -m pytest --cov=src reports TOTAL 686 0 100%).
Provenance python -m pytest --cov=src --cov-report=term-missing output.
Quality checks python -m pytest -q passes (332 passed 1 skipped); python -m pytest --cov=src shows 100% total coverage.
Reviewer GitHub Copilot Task Agent


2026-04-29 (export live research records feature)
Change type add
Scope scripts config docs governance tests
Files affected config/research_queries.yml scripts/export_live_research_records.py tests/test_export_live_research_records.py docs/ONE_CLICK_RESEARCH_RUNBOOK.md CHANGELOG.txt
Summary Added scripts/export_live_research_records.py to export Crossref and other provider metadata based on 12 canonical blue economy sector queries defined in config/research_queries.yml. The [...]
Reason The Crossref provider is fully implemented; the next step is to export live metadata into outputs/research_sources without changing the core competence model yet. This enables evidence col[...]
Impact Researchers can now fetch structured literature metadata for 12 blue economy sectors (offshore energy, ports, maritime transport, fisheries, aquaculture, coastal tourism, marine biotechnol[...]
Provenance Crossref REST API documentation https://www.crossref.org/documentation/retrieve-metadata/rest-api/, EU Sustainable Blue Economy framework sector definitions, University of Szczecin Blu[...]
Quality checks python -m pytest tests/test_export_live_research_records.py passed (20 tests); offline mode smoke test successful; outputs/research_sources directory structure verified.
Reviewer GitHub Copilot Task Agent

2026-04-29 (research secrets setup documentation and audit output alignment)
Change type fix update
Scope scripts docs governance
Files affected scripts/bootstrap_research_secrets.ps1 scripts/audit_research_api_config.py COPILOT_MCP_SETUP.md docs/RESEARCH_API_CICD_SETUP.md MANIFEST_SOURCES.csv
Summary Restored scripts/bootstrap_research_secrets.ps1 (ASCII-safe for Windows PowerShell 5.1 and PowerShell 7) with correct backend alias normalisation (DotEnv, Gcp, Github, UserEnv, user-env),[...]
Reason Windows manual setup needed a stable PowerShell loading path and the MCP setup runbook did not provide enough portal-level onboarding guidance for academic API access.
Impact Windows users can reliably dot-source generated `.env.ps1` files across PowerShell versions, and the documented secret bootstrap flow now matches the supported backend names and current pr[...]
Provenance User findings on COPILOT_MCP_SETUP.md and helper output naming; official provider setup references for Crossref, Elsevier Scopus, SciVal, and Clarivate.
Quality checks python -m pytest tests/ passed; MANIFEST_SOURCES.csv updated.
Reviewer GitHub Copilot Coding Agent

2026-04-28 (PowerShell research run fixes and UTF-8 validation hardening)
Change type fix update add
Scope scripts infra governance
Files affected scripts/run_research_api_full.ps1 scripts/validate_generated_outputs.py outputs/research_source_capabilities.json MANIFEST_SOURCES.csv
Summary (1) Fixed Python version guard in run_research_api_full.ps1: corrected capture-group indices (Groups[1]/Groups[2] instead of Groups[2]/Groups[3]) so Python 3.10+ is now enforced correctly[...]
Reason The Python version guard was accepting Python 3.0–3.9 due to off-by-one capture-group indexing; Step 5 was silently skipped; validate_generated_outputs.py could fail on Windows due to en[...]
Impact run_research_api_full.ps1 now correctly blocks Python <3.10 and executes the live smoke test when -Live is passed; validate_generated_outputs.py is encoding-safe on all platforms.
Provenance PR review comments from copilot-pull-request-reviewer; CI governance-and-repro failure logs.
Quality checks python -m pytest tests/ passed; MANIFEST_SOURCES.csv regenerated and committed.
Reviewer GitHub Copilot Coding Agent

2026-04-27 (Python version policy)
Change type update fix
Scope ci policy
Files affected .github/workflows/ci.yml pyproject.toml
Summary Updated CI test-suite matrix from Python 3.9/3.11/3.12 to 3.10/3.11/3.12. Updated pyproject.toml: requires-python = ">=3.10", classifiers updated to reflect 3.10/3.11/3.12, black target-v[...]
Reason sentence-transformers==5.4.1 (pinned in requirements.txt to address CVE-2024-3568) does not support Python 3.9; CI was failing on the 3.9 matrix leg during dependency installation. The NLP[...]
Impact CI is now green across the 3.10/3.11/3.12 matrix. Python 3.9 is no longer a supported runtime. This is a breaking change for users running Python 3.9; upgrade to Python 3.10+ is required.
Provenance Review comment 4181625551 (robertbartlomiejski); requirements.txt pinning sentence-transformers==5.4.1.
Quality checks python -m pytest tests/ passed (291 tests) on Python 3.12 (sandbox); CI matrix updated to 3.10/3.11/3.12.
Reviewer GitHub Copilot Coding Agent

2026-04-27 (hardening pass)
Change type update fix add
Scope scripts infra docs ci safety
Files affected cloudbuild.yaml cloudbuild.live.yaml scripts/bootstrap_research_secrets.sh scripts/bootstrap_research_secrets.ps1 scripts/run_research_api_full.ps1 scripts/run_cloudbuild_research_[...]
Summary Hardening pass (review comment 4181399408): (1) Split cloudbuild.yaml into offline-only file (no availableSecrets/secretEnv — safe to run without Secret Manager versions) and cloudbuild[...]
Reason Review comment 4181399408 (robertbartlomiejski): offline Cloud Build must not require secrets, PowerShell equivalents needed for Windows workflow, user-env bootstrap was misleading, gcloud[...]
Impact cloudbuild.yaml is now safe to run without Secret Manager versions; bootstrap script no longer silently suppresses gcloud failures; Windows users have full PowerShell parity; Terraform sup[...]
Provenance Review comment 4181399408 on PR#101.
Quality checks python -m pytest tests/ passed (291 tests); smoke_scientific_bridge.py --offline passed.
Reviewer GitHub Copilot Coding Agent

2026-04-27
Change type add update
Scope scripts infra docs testing ci governance
Files affected scientific_bridge.py src/scientific_sources/ src/nlp_reliability/ infra/gcp-mirror/ cloudbuild.yaml scripts/bootstrap_research_secrets.sh scripts/run_research_api_full.sh scripts/a[...]
Summary Implemented Issue #100 (all refinement comments): (1) Modular provider architecture in src/scientific_sources/ with CrossrefProvider, ElsevierScopusProvider, WebOfScienceProvider, SciValP[...]
Reason Issue #100 with refinement comments from repository owner: provider system, GCP mirror, one-click bootstrap, runbook, safety gitignore rules.
Impact scientific_bridge.py still works as MCP server; Crossref always configured; proprietary providers capability-gated; no committed secrets; CI remains green without API keys; 53 new tests pa[...]
Provenance Issue #100 and all 4 refinement comments (IDs 4327013628, 4327058852, 4327130546, 4327198583).
Quality checks python -m pytest tests/ passed (all new and existing tests); smoke_scientific_bridge.py --offline passed.
Reviewer GitHub Copilot Coding Agent

2026-04-25
Change type fix update add
Scope scripts testing
Files affected scripts/validate_generated_outputs.py tests/test_validate_generated_outputs.py
Summary Post-review fixes to the semantic validator: (1) Added ERRORS.clear() and WARNINGS.clear() at start of main() so repeated in-process calls are independent. (2) Added loud schema validatio[...]
Reason Review comment 4175248362 identified: unsafe global state for repeated main() calls, silent schema failures via .get() defaults, and a misleading OK EQF-level message emitted even when a s[...]
Impact Validator is now safe for in-process repeated calls, fails loudly on malformed JSON/CSV, and does not mislead with spurious OK messages.
Provenance PR#98 review comment 4175248362; scripts/validate_generated_outputs.py; tests/test_validate_generated_outputs.py.
Quality checks python scripts/validate_generated_outputs.py passed; python -m pytest tests/test_validate_generated_outputs.py passed (20 tests); python -m pytest tests/ passed (238 passed, 1 skip[...]
Reviewer GitHub Copilot Coding Agent

2026-04-25
Change type add update
Scope scripts testing ci outputs
Files affected outputs/ scripts/validate_generated_outputs.py tests/test_validate_generated_outputs.py .github/workflows/full-analysis.yml
Summary Regenerated all committed outputs from post-PR#95/PR#97 sector-aware model: rm -rf outputs && python run_full_analysis.py. Added scripts/validate_generated_outputs.py, a semantic validato[...]
Reason outputs/ was stale: gaps_summary.csv showed identical values for all 12 sectors and credentials_database.json reused the same global literature IDs across sectors, both inconsistent with t[...]
Impact outputs/ now reflects sector-differentiated gap counts, sector-valid EQF6/EQF7 literature competences, and 12 distinct sector TMBD dictionaries. CI will block any future logic change that [...]
Provenance PR#95 (sector-scoped extract_literature_competences / run_gap_analysis), PR#97 (sector-filtered generate_micro_credentials); issue #98.
Quality checks python run_full_analysis.py succeeded; python scripts/validate_generated_outputs.py passed all checks; python -m pytest tests/ passed (228 passed, 1 skipped).
Reviewer GitHub Copilot Coding Agent

2026-04-25
Change type fix update add
Scope scripts testing
Files affected run_full_analysis.py tests/test_run_full_analysis.py
Summary Fixed generate_micro_credentials to use sector-aware literature selection: replaced global literature[:5] slice with sector_lit_ids derived from gap.required_ids filtered by dimension=="l[...]
Reason generate_micro_credentials previously selected literature[:5] globally, bypassing the sector filter introduced by PR #95 in run_gap_analysis. This could insert out-of-sector literature com[...]
Impact Micro-credential competence lists now respect sector boundaries for literature-derived competences, consistent with gap analysis and sector dictionary semantics.
Provenance PR #95 and post-merge review; problem statement for PR #97.
Quality checks python -m pytest tests/test_run_full_analysis.py passed; python -m pytest tests/ passed.
Reviewer GitHub Copilot Coding Agent

2026-04-25
Change type fix update add
Scope scripts testing
Files affected run_full_analysis.py tests/test_run_full_analysis.py
Summary Fixed sector-specific literature competence mapping in run_full_analysis.py: added _THEME_SECTORS lookup (36 of 52 themes mapped to primary sectors), updated extract_literature_competence[...]
Reason All 12 sector TMBD dictionary JSONs were identical because extract_literature_competences hardcoded sectors=SECTORS for every competence. run_gap_analysis also treated all literature compe[...]
Impact Sector dictionaries now differ per sector. Gap analysis, micro-credential generation, and sector pathways now apply sector-specific literature filtering, consistent with sector dictionary [...]
Provenance PR #95 and PR #96 review comments (robertbartlomiejski, copilot-pull-request-reviewer); _LIT_THEMES universe in run_full_analysis.py; canonical SECTORS list.
Quality checks python -m pytest tests/test_run_full_analysis.py passed (6 new regression tests); python -m pytest tests/ passed (217 passed, 1 skipped); MANIFEST_SOURCES.csv regenerated and uncha[...]
Reviewer GitHub Copilot Coding Agent

2026-04-20
Change type add update fix
Scope testing infrastructure governance
Files affected .github/workflows/ci.yml tests/test_core.py tests/test_load_real_competences.py tests/test_generate_manifest.py tests/test_edge_cases.py tests/fixtures/sample_competences.csv tests[...]
Summary Expanded test coverage from 22 to 104 tests and integrated test suite into CI/CD pipeline with multi-version Python testing (3.9, 3.11, 3.12). Added test-suite job to .github/workflows/ci[...]
Reason Fulfill requirement to integrate test suite into CI/CD pipeline for safe refactoring and feature development while maintaining TMBD framework integrity. Address PR review feedback on test [...]
Impact CI/CD pipeline now runs 104 tests across 3 Python versions on every push and pull request. Coverage significantly improved: src/core.py (4→24 tests), src/competence_mapper.py (4→17 tes[...]
Provenance PR review comments from copilot-pull-request-reviewer; tests/fixtures/blue_social_competences_sample.csv based on data/derived/Blue Social Competences Univ Szczecin - Overall Blue Comp[...]
Quality checks pytest tests/ 104/104 passed; all review comments addressed; CI manifest reproducibility check passes; test isolation verified (no temp files left in repo tree); Excel fixture forc[...]
Reviewer GitHub Copilot Coding Agent

2026-04-02
Change type add update fix
Scope infrastructure pipeline data
Files affected deep_update_patch/load_real_competences.py (new) deep_update_patch/main_real_data.py deep_update_patch/run_full_analysis.py deep_update_patch/src/competence_mapper.py deep_update_p[...]
Summary Added missing deep_update_patch/load_real_competences.py providing a plug-and-play three-matrix loader (baseline, sector, cluster CSVs). Fixed both entrypoints to pass the correct CSV fil[...]
Reason Addresses all substantive and secondary review blockers raised in PR #37 review (https://github.com/robertbartlomiejski/morskamary/pull/37#pullrequestreview-4049795157). Primary blockers: [...]
Impact Entrypoints deep_update_patch/main_real_data.py and deep_update_patch/run_full_analysis.py now run without error and load all three CSV matrices. Sector gap analysis is correct for non-nor[...]
Provenance data/derived/Blue Social Competences Univ Szczecin - Overall Blue Competences Dimension.csv; data/derived/combined_blue_economy_labor_justice.csv; data/derived/combined_blue_economy_re[...]
Quality checks deep_update_patch/tests/test_core.py passes (8 tests). Both entrypoints run smoke-tested successfully. HTML output validated for proper escaping of R&I and Infra & Robotics labels.[...]
Reviewer GitHub Copilot Coding Agent


2026-04-04
Change type fix update add
Scope scripts infrastructure security
Files affected load_real_competences.py run_full_analysis.py security/security-checklist.md (new) .github/workflows/full-analysis.yml (new) outputs/ (regenerated)
Summary Fixed flake8 F541 warnings (bare f-strings without placeholders) and W293 whitespace-in-blank-lines in load_real_competences.py. Formatted both load_real_competences.py and run_full_analy[...]
Reason Address PR #43 review feedback: (1) apply security checklist validation, (2) improve readability and eliminate bare f-strings, (3) ensure HTML reports escape user-derived content per the c[...]
Impact All four HTML reports now contain valid, well-escaped HTML. The full-analysis.yml CI workflow enables on-push and workflow_dispatch execution of the complete pipeline with artifact upload.[...]
Provenance data/derived/Blue Social Competences Univ Szczecin - Overall Blue Competences Dimension.csv; data/derived/combined_blue_economy_labor_justice.csv; data/derived/combined_blue_economy_re[...]
Quality checks flake8 load_real_competences.py run_full_analysis.py: 0 findings; black --check: reformatted successfully; python run_full_analysis.py: generated all 8 outputs; pytest tests/ 8/8 p[...]
Reviewer GitHub Copilot Coding Agent

2026-04-01
Change type add update
Scope infrastructure documentation governance scripts
Files affected .github/repository-guardrails.instructions.md (new) Deploy-CopilotSynergy.ps1 scientific_bridge.py COPILOT_MCP_SETUP.md README.md CHANGELOG.txt
Summary Added repository guardrails policy file at .github/repository-guardrails.instructions.md establishing hard constraints for coding agents: real-data precedence (main_real_data.py over demo[...]
Reason Strengthen repository governance by establishing clear execution hierarchy and preventing scope creep of optional tooling into core infrastructure. Respond to maintainer feedback requestin[...]
Impact Coding agents now have explicit instructions to prioritize main_real_data.py for substantive outputs, treat demo scripts as validation-only, avoid introducing Node.js as core dependency, a[...]
Provenance main_real_data.py (real-data primary script), demo_workspace_instructions.py (validation script), requirements.txt (Python-only core dependencies), README.md (Quick Start hierarchy), m[...]
Quality checks All updated files validated for consistency; README.md Quick Start section reordered with real-data first; all MCP files include "optional tooling" disclaimers; guardrails policy r[...]
Reviewer GitHub Copilot Coding Agent

2026-04-01
Change type add
Scope infrastructure documentation scripts
Files affected Deploy-CopilotSynergy.ps1 scientific_bridge.py COPILOT_MCP_SETUP.md .vscode/mcp_full_integration.json
Summary Added comprehensive MCP (Model Context Protocol) infrastructure for GitHub Copilot integration, enabling full-context access to local repositories, SharePoint, Google Drive, and scientifi[...]
Reason Enable "Full Data Use" architecture for GitHub Copilot Coding Agent, providing direct access to morskamary and maritimesociology repositories, University of Szczecin SharePoint synchronize[...]
Impact Researchers can now use GitHub Copilot with full contextual awareness of TMBD framework, Blue Social Competences, literature sources, and institutional data. Scientific claims are automati[...]
Provenance LLM_CONTEXT_INSTRUCTION.txt (TMBD definitions), DATA_GOVERNANCE.txt (FAIR/CARE principles), .github/copilot-instructions.md (coding conventions), README.md (project overview). Scientif[...]
Quality checks Deploy-CopilotSynergy.ps1 syntax verified with PowerShell 7.x; scientific_bridge.py tested with Python 3.9+ and mypy type checking; COPILOT_MCP_SETUP.md reviewed for accuracy again[...]
Reviewer GitHub Copilot Coding Agent

2026-03-22
Change type fix add
Scope scripts governance CI
Files affected .github/workflows/CI (deleted) run_full_analysis.py (new) outputs/.gitkeep (new) .gitignore (updated) CHANGELOG.txt
Summary Deleted the invalid .github/workflows/CI workflow file (no .yml extension, malformed YAML missing top-level name:/on:/jobs: keys) since the correct ci.yml already existed. Created run_ful[...]
Reason The CI workflow file had two independent defects: (1) no .yml/.yaml extension so GitHub Actions would not reliably recognise it; (2) the YAML was structurally invalid starting with a bare [...]
Impact All outputs are now generated by run_full_analysis.py in outputs/ (excluded from git via .gitignore). CI is now handled exclusively by ci.yml.
Provenance data/derived/Blue Social Competences Univ Szczecin - Overall Blue Competences Dimension.csv; data/derived/combined_blue_economy_labor_justice.csv; data/derived/combined_blue_economy_re[...]
Quality checks flake8 run_full_analysis.py passed; pytest tests/ 8/8 passed; run_full_analysis.py executed successfully generating all 8 output files.
Reviewer GitHub Copilot Coding Agent

2026-03-09
Change type fix add
Scope scripts governance
Files affected requirements.txt Dockerfile CONTRIBUTING.md
Summary Fixed openpyxl version constraint in requirements.txt (changed >=3.8.0 to >=3.0.0; version 3.8.0 does not exist). Fixed Windows-style path in Dockerfile CMD (backslash → forward slash).[...]
Reason requirements.txt blocked pip install entirely; Dockerfile CMD would fail on Linux containers; no onboarding guide existed for new contributors.
Impact pip install -r requirements.txt now succeeds; Docker build runs correctly; new contributors can set up and verify the environment in minutes.
Provenance None (bug fixes and new documentation only).
Quality checks pip install verified; pytest tests/ passes (8/8); CONTRIBUTING.md reviewed for accuracy against pyproject.toml and existing tooling.
Reviewer GitHub Copilot Agent

2026-02-18
Change type add
Scope governance
Files affected CITATION.txt DATA_GOVERNANCE.txt LLM_CONTEXT_INSTRUCTION.txt CHANGELOG.txt
Summary Added repository governance and citation scaffolding to support reproducible competence mapping and micro-credential design.
Reason Establish traceability rules and evidence discipline for Blue Sociology outputs.
Impact Improves auditability of future derived datasets and manuscript revisions.
Provenance None (new files created from scratch).
Quality checks Manual review for internal consistency and alignment with repository sources referenced in the governance files.
Reviewer Robert Bartłomiejski
text

2026-06-30
Change type fix update
Scope static-quality linting typing CI documentation
Files affected .flake8 pyproject.toml .github/workflows/ci.yml CONTRIBUTING.md main.py main_real_data.py scientific_bridge.py scripts/build_derived.py scripts/build_tmbd_dictionary.py scripts/convert_pdfs_to_txt.py scripts/cumulative_update_merge_full_live_enriched_and_static_all_outputs_fragmented_analysis.py scripts/smoke_scientific_bridge.py scripts/sync_audit.py src/nlp_reliability/triangulation.py src/scientific_sources/elsevier_scopus.py src/scientific_sources/scival.py src/scientific_sources/web_of_science.py tests/test_build_derived.py tests/test_ci_workflow.py tests/test_core.py tests/test_edge_cases.py tests/test_export_live_research_records.py tests/test_nlp_reliability.py tests/test_pipeline_matrix.py tests/test_sync_audit.py
Summary PR-181 baseline cleanup: resolved repository lint/type root causes in maintained code, added explicit legacy/demo exclusions for non-production artifacts, and added canonical CI static-quality gate running module-based flake8 and mypy.
Reason Separate baseline static-quality hardening from PR-179/180 feature/changelog work and prevent recurrence through canonical CI enforcement.
Impact `python -m flake8 .`, `python -m mypy src scripts run_full_analysis.py main.py`, and `python -m pytest tests/ -v` are now aligned across local workflow and CI.
Reviewer GitHub Copilot Coding Agent

2026-07-01
Change type fix
Scope gap-model provenance analysis
Files affected src/gap_model.py run_full_analysis.py tests/test_gap_model.py
Summary Fixed provider provenance (no longer uses comp.source.authors as provider); fixed dynamic CSV header detection in _collect_supply_from_microcredentials_csv; added item-level match provenance fields (matched_supply_id, matched_supply_origin, match_method, match_score) to GapEvidence.
Reason Authors ("Smith, J.; Jones, K.") are bibliographic metadata, not data providers. Provider must identify the data/source provider (crossref, scopus, wos, baseline, etc.). CSV header was hard-coded at row index 2; now dynamically located by searching for the first row whose first cell equals "Dimension" and which contains a known sector header. Item-level match provenance improves auditability of gaps_detailed.json.
Impact _extract_provider_from_comp now returns (1) a live-API provider slug from a lit_live_<provider>_NNNNN ID (e.g. "scopus"), or (2) an explicit provider:/source_provider:/data_provider: keyword value, otherwise "unknown"—bibliographic authors, support: keywords, and thematic keywords (labor_justice, research_gaps, oceanic, etc.) are never used as provider. CSV parsing is robust to BOM rows, shifted headers, and extra introductory rows. Covered demand items now carry matched_supply_id, matched_supply_origin, match_method, and match_score.
Provenance PR addressing review comment #4608688805 on PR #182.
Quality checks pytest tests/ passes (757/757).
Reviewer GitHub Copilot Coding Agent

2026-07-08
Change type fix
Scope governance CI run-archive
Files affected outputs/run_archive/runs/28625632237-1/manifest.json
Summary Backfilled archived run manifest with six required fields introduced by PR-186 (analysis_timestamp_utc, analysis_input_mode, is_static_recovery_mode, static_recovery_reason, allow_static_recovery_mode_env, provider_set). The manifest was generated before PR-186 added these fields to the schema, causing validate_run_archive_integrity.py to fail in CI.
Reason Schema validation in CI (validate_run_archive_integrity.py) uses a strict required-properties list. The archived manifest was created before the live-enriched-default policy fields were added. Backfilling with correct values (live mode, no static recovery) restores CI integrity without altering the provenance of archived data.
Impact CI governance-and-repro job passes; run archive integrity check passes.
Quality checks python scripts/validate_run_archive_integrity.py --archive-root outputs/run_archive → passed.
Reviewer GitHub Copilot Coding Agent

2026-07-10
Change type add
Scope scripts source-modules documentation tests workflow
Files affected src/scientific_sources/derived_competence_analysis.py scripts/build_layer4_5_scientific_analysis.py scripts/compute_live_novelty_metrics.py scripts/build_statistical_research_report.py scripts/build_live_cumulative_release_package.py docs/STATISTICAL_REPORT_METHODOLOGY.md docs/LIVE_CUMULATIVE_SCIENTIFIC_DATABASE.md tests/test_derived_competence_analysis.py tests/test_compute_live_novelty_metrics.py tests/test_live_cumulative_release_package.py
Summary PR-190 Task C: added Layer 4 derived competence-demand model, Layer 5 sector gap model + EQF 4-7 credential translation + learning outcomes, novelty gates A-E evaluator, statistical HTML report + methodological audit, and browser-downloadable release ZIP package with deterministic SQLite. Applied the mandated demand-strength formula (0.30/0.20/0.20/0.15/0.15 weights) and enforced the growth-eligibility reliability rule (duplicate_only excluded from growth metrics). Separated static-baseline availability from live availability in the gap model (Gate D fails on contamination).
Reason Complete the live cumulative scientific database with statistical validity, credential translation, and a reproducible downloadable package so that the pipeline delivers auditable Blue Economy competence-demand evidence end to end.
Impact `outputs/cumulative_database/` now contains derived_competence_demands, sector_axis_gap_model, credential_translation_eqf4_7, learning_outcomes, layer4/5_manifest, layer_readiness_report, and VARIABLE_LABELS/VALUE_LABELS; `reports/` contains morskamary_statistical_report.{html,pdf} and morskamary_methodological_audit.html; `outputs/release_packages/morskamary_live_cumulative_latest.zip` is the single-file consumer bundle.
Quality checks pytest tests/test_derived_competence_analysis.py tests/test_compute_live_novelty_metrics.py tests/test_live_cumulative_release_package.py → 15/15 passed. End-to-end smoke: L2-3 → L4-5 → gates (pass) → report (HTML+audit) → package (20 files, SQLite generated, byte-identical rebuild).
Reviewer GitHub Copilot Coding Agent
