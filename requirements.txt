# Core dependencies for morskamary Blue Sociology project
pandas>=1.5.0
openpyxl>=3.8.0
pyyaml>=6.0
pytest>=7.0

# Optional development dependencies (install with: pip install -r requirements.txt[dev])
# black>=23.0
# flake8>=6.0
# mypy>=1.0
